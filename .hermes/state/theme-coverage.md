# theme-coverage — every installed GUI/TUI on elster paints from cells/common/theme.nix (Kanagawa Wave), or is on a written exceptions list

Status: PLANNING (2026-09). Owner: user; Claude = executor.
Prereq: the-hive at 0251f2f or later (palette in `cells/common/theme.nix`, read as `inputs.cells.common.theme`). User runs every rebuild; Claude only edits + evals.

Target: each row of the Matrix below is `themed` (colours trace to `cells/common/theme.nix`), `inherits` (verified to pick up foot ANSI / GTK / Qt without its own config), or `exception` (with the reason). No row stays `?`. Stretch: desktop consumers read `theme.roles.*` only, never `theme.colors.*` (58 refs in 8 files today), so switching the palette is one file.
Source refs: `cells/common/theme.nix` (colors/roles/ansi/name); GTK `home/desktop/gtk.nix` and Qt `home/desktop/qt.nix` (the two inherited toolkits); `deck/homeModules/*.nix` (shell/TUI tools); `home/dev/agents.nix` (claude settings.json :144, opencode tui :126); `profiles/agent-proxy.nix` (hermes install); `packages.nix` (nixpak Slack/Telegram, grayjay, horizon-gm FHS); `profiles/auth-entra.nix` (o365 PWAs via Edge); `dotfiles/nvim/lua/custom/plugins/colorscheme/` (catppuccin: 191 `C.*` refs in hl_overrides.lua, 24 lines in ui/editor/bufferline, + init.lua, lazy.lua, lazy-lock.json). Upstream themes: `rebelot/kanagawa.nvim` `extras/tmTheme/kanagawa.tmTheme` (bat/delta) and `lua/kanagawa/themes.lua` wave `diff = { add = winterGreen, delete = winterRed, change = winterBlue, text = winterYellow }`; opencode `packages/ui/src/theme/themes/kanagawa.json`; hermes skins = `~/.hermes/skins/<name>.yaml` + `display.skin` (schema in hermes_cli/skin_engine.py); Claude Code 2.1.239 lists "custom themes" in `--safe-mode` help (location unverified).
Repo: HM release-25.05 (`cells/workstation/flake.nix:34`): 25.05 option names (`programs.git.delta`). Format `nix run nixpkgs#alejandra`. Any `inputs.cells.common.*` read loads globals.nix.age: eval needs a warm TPM cache.
Naming: palette additions `colors.winterGreen = "2b3328"`, `winterRed = "43242b"`, `winterBlue = "252535"`, `winterYellow = "49443c"` (verbatim kanagawa.nvim); roles `diffAdd`, `diffDelete`, `diffChange`, `diffText`, `success`, `warning`, `info`. Upstream source `kanagawaSrc` (fetchFromGitHub rebelot/kanagawa.nvim, rev pinned in 2.1). Theme/skin name `kanagawa` everywhere a tool takes a name (bat, btop, hermes skin, claude, opencode, zathura).

Invariants:
- Existing palette values never change: `nix eval --impure --json --expr 'removeAttrs (import ./cells/common/theme.nix {}) ["name"]'` keeps every key/value from 0251f2f; additions only.
- `nix run nixpkgs#alejandra -- --check <touched .nix>` == exit 0.
- `nix eval --raw .#nixosConfigurations.elster.config.system.build.toplevel.drvPath` succeeds (user runs it when the TPM cache is cold).
- Tor Browser is never themed: any change to its look breaks the uniform fingerprint.
- One phase = one Conventional Commit with why-body + Co-Authored-By: Claude; nothing pushed by Claude.
- User acceptance gate: a row flips to `themed` only after the USER has tested it on the rebuilt system and said so. Claude's evals/screenshots are not acceptance. After every rebuild that touches a row, Claude stops and hands the user a per-app test checklist (what to open, which screen, what to look at: bg, fg, selection, diff/err colours, popups/dialogs), then waits. The phase is not ✅ and the next phase does not start while any of its rows is `awaiting user test`. The user's verdict + date goes into Progress; a failed test sets the row back to `wrong` with the note.

## Matrix (Phase 0 fills `status`; phases flip it)
Status flow for every row a phase changes: `wrong`/`default` → `awaiting user test` (after rebuild + checklist handed over) → `themed` (user said OK) or back to `wrong` (user said no, note why).
Inventory source: `.desktop` files in /run/current-system/sw, /etc/profiles/per-user/crookedmirror, ~/.nix-profile (Entra), plus TUIs from home.packages. Kind: G = GUI, T = TUI/CLI with colour.

| app | kind | mechanism | status | phase |
|---|---|---|---|---|
| dwl, waybar, fuzzel (+power/calc/wifi menus), mako, swaylock, swaybg, slurp, satty | G | palette in Nix | themed | – |
| foot, tmux, tuigreet + VT | T | palette in Nix | themed | – |
| Thunar, pwvucontrol, xdg-desktop-portal-gtk dialogs | G | GTK (gtk.nix) | inherits (0.2: Thunar bg #16161d/#1f1f28, pwvucontrol #1f1f28 + #7e9cd8 accent; portal = same GTK3 theme as Thunar, not opened) | – |
| qBittorrent, qt5ct/qt6ct | G | Qt (qt.nix) | inherits (0.2: qBt #1f1f28/#16161d, qt6ct #181820/#393946) | – |
| KeePassXC | G | `[GUI] ApplicationTheme=classic` via activation → qt6ct palette | themed (user OK 2026-09) | 5 |
| Vial | G | PyInstaller Qt, xcb only (dies on Wayland), ignores qt5ct | exception (sets its own hardcoded palette, themes.py:248; settings unpersisted; rare use — user 2026-09) | – |
| Horizon (gm) | G | omnissa `horizon-client_wrapper:2` hard-sets `GTK_THEME=Adwaita` (runs after our launcher's env, so only a patched runScript can change it, 7.6); horizon-gm also sets `XDG_CONFIG_HOME=~/.omnissa/xdg-gm` (no gtk-3.0 there) | exception (user 2026-09: "не стоит"; client hard-paints its greys + cairo-black captions, and our theme blanked the disconnect dialog text → reverted to stock Adwaita, 7.6) | – |
| Zathura | G | programs.zathura options from roles | themed (user OK 2026-09) | 5 |
| mpv (OSD), umpv | G | osd-* + script-opts/osc.conf from roles | themed (user OK 2026-09) | 5 |
| avizo (volume/brightness OSD) | G | config.ini from roles; callers pass -d (grey icons) | themed (user OK 2026-09) | 5 |
| Firefox, Microsoft Edge | G | Dark Reader forced (built-in Kanagawa scheme; settings = import of ~/.config/darkreader/kanagawa.json, generated from roles, once per browser); Fx chrome = userChrome.css from roles linked into every profile by HM activation (7.1b); Edge chrome = exception; own static theme impossible (signing) | Fx chrome themed (user OK 2026-09, incl. primary-button fix); content = Dark Reader Kanagawa in Firefox + Edge, themed (user OK 2026-09, 7.2) | 7 |
| o365 PWAs (Teams, Outlook, Word, Excel, PowerPoint, OneNote, OneDrive, SharePoint) | G | NOT Edge: rust_o365 wraps teams-for-linux 2.17.1 (Electron), profiles in ~/.config/o365-profiles | partial (7.4, user 2026-09: Teams = its own dark, Outlook/Office web = Microsoft's light; palette would need customCSSLocation, not taken) | 7 |
| Slack, Telegram (nixpak) | G | own in-app themes | Slack partial (user screenshot 2026-09: own dark #1a1d21 + catppuccin mauve #cba6f7 selection); Telegram wrong (not screenshotted) | 7 |
| Mattermost | G | Electron, own theme | wrong (0.2: light default) | 7 |
| SimpleX | G | Compose/skiko, own theme | exception (theme import lives in unpersisted ~/.config/simplex; built-in dark — user 2026-09) | – |
| Grayjay | G | CEF, own theme | exception (no theme setting, CSS baked in wwwroot; own dark #1b1b1b — user 2026-09) | – |
| Tor Browser | G | – | exception | – |
| GVim | G | own GUI colorscheme; `vim` is only in base.nix as a rescue editor | exception (rescue tool, not used) | – |
| cups web UI, NixOS manual | G | web pages in the browser | exception (covered by Dark Reader, row Firefox/Edge) | – |
| vim | T | default colorscheme = 16 ANSI | inherits (by construction) | – |
| neovim | T | kanagawa.nvim wave + hl_overrides via palette.lua | themed (user OK 2026-09) | 4 |
| bat, delta, lazygit diffs | T | bat tmTheme + delta styles | themed (user OK 2026-09, Markdown after 2.4) | 2 |
| btop | T | themes/kanagawa.theme generated from palette | themed (user OK 2026-09) | 3 |
| Claude Code | T | ~/.claude/themes/kanagawa.json (72 keys) + settings `custom:kanagawa` | themed (user OK 2026-09) | 6 |
| Hermes | T | skin yaml linked by seedHermes + `display.skin`; banner art recoloured at build; diff bgs computed; pt chrome 256-colour | themed (user OK 2026-09 after 6.5 patch; diff bgs computed, pt chrome 256-colour, out-of-credits panel #CD7F32 = accepted off-palette) | 6 |
| opencode | T | own themes/kanagawa.json (built-in is off-palette) + tui.json theme | themed (user OK 2026-09; logo shading hardcoded) | 6 |
| rmpc, bluetui, eza, dircolors/ls, fast-syntax-highlighting, jq, rg | T | ANSI → foot | inherits (0.2: only 16-colour SGR / 256 idx < 16) | – |
| lazygit UI | T | ANSI → foot | themed (authorColors = info; user OK 2026-09) | 2 |
| p10k | T | dotfiles/zsh/.p10k.zsh, ANSI 1-7 | inherits (PERLBREW_FOREGROUND 67 → 6, same as PLENV; not observable without perlbrew) | 5 |
| skim, fzf, Ctrl-R (histdb-skim) | T | one `--color` spec from roles: SKIM_DEFAULT_OPTIONS + HISTDB_COLOR; fzf `colors` | themed (user OK 2026-09) | 5 |
| nyx | T | curses, 16 ANSI | inherits (not observed: needs a Tor ControlPort, crookedmirror only) | – |
| nmtui (newt) | T | NEWT_COLORS (ANSI names → foot palette), deck/homeModules/newt.nix | themed (user OK 2026-09) | 5 |
| man/less, fastfetch, git status/log, typst/tinymist, top | T | ANSI → foot | inherits (0.2: 16-colour only; man = bold/underline) | – |

## Phase 0 — Inventory + recon ✅
0.1 Regenerate the inventory (the `.desktop` scan for both accounts + `ls ~/.nix-profile/bin`) and diff against the Matrix; add missing rows, drop uninstalled ones. — DONE 2026-09, `ls` of the 5 applications dirs + both profiles' bin: GUI set = Matrix; split KeePassXC/Vial and the chat row, added vim/jq/rg/typst/top; no htop installed; `hermes` is system-wide (sw/bin), nyx/tor-browser crookedmirror only, horizon/Grayjay/Slack/Telegram vkokurin only.
0.2 For every `inherits?`/`?` row, one observation each, recorded in the row: GTK/Qt apps via screenshot (grim) of the window; TUIs via `script -qc '<tool>' | grep -o $'\e\[38;2;[0-9;]*m'` (truecolor escapes = own palette, not foot's) or a screenshot. — DONE 2026-09: GUIs on a headless dwl (`WLR_BACKENDS=headless dwl -s ...` → wayland-1, grim + imagemagick pixel/histogram; xcb-only apps via DISPLAY=:1); TUIs in a private `tmux -L tc0`, `capture-pane -e`, SGR histogram (fg16/256/#rgb). Results in the Matrix rows.
0.3 Horizon: `horizon-gm` then check `GTK_THEME`/`XDG_DATA_DIRS` inside the FHS env (`cat /proc/<pid>/environ`), screenshot. — DONE 2026-09: environ has `GTK_THEME=Adwaita` (from nixpkgs `omnissa-horizon-files-2605/bin/horizon-client_wrapper:2`, `export GTK_THEME='Adwaita'`) and `XDG_CONFIG_HOME=/home/vkokurin/.omnissa/xdg-gm` (our horizon-gm wrapper; no gtk-3.0 inside). Screenshot = light Adwaita. Phase 7.6 (audit: the wrapper sets GTK_THEME last, so patch its runScript) needs both: override GTK_THEME after the wrapper and give xdg-gm a gtk-3.0 (or the theme via GTK_THEME=Kanagawa:dark + XDG_DATA_DIRS).
0.4 Tool capabilities — DONE 2026-09:
  - bat: yes. HM pin 44831a7 `modules/programs/bat.nix:74` `themes = mkOption` ({src, file}).
  - btop: yes, `programs.btop.themes` (`btop.nix:63`, writes btop/themes/<name>.theme). rmpc also has `programs.rmpc` on this pin (the "no HM module" note in music/default.nix:9 is stale).
  - opencode 1.18.21: yes, built-in `kanagawa` (`strings .opencode-wrapped | grep -i kanagawa` → "kanagawa", kanagawa.json).
  - Claude Code 2.1.239: yes. `strings .claude-wrapped`: themes dir = `<config dir>/themes/*.json` (≤256KB, hot-reloaded), schema `{name, base: dark|light|…, overrides: {token: "#rrggbb"|"rgb()"|"ansi256(n)"|"ansi:<name>"}}`, selected as `theme: "custom:<slug>"` in settings.json; confirmed by a third-party blog (claudcod.com) + tinted-theming/tinted-claude-code README, not by official docs.
  - hermes v0.20.5: yes. skin_engine.py docstring: `~/.hermes/skins/<name>.yaml`, `display.skin: <name>` in config.yaml or `/skin`; `hermes config get display.skin` → `default`. config.yaml is Hermes-owned (agent-proxy.nix:56), so set it through `hermes config set` in the seed script, not a symlink.
Exit criteria: no `?` left in the Matrix `status` column; 0.4 answered yes/no with the command used; Matrix committed.

## Phase 1 — Palette additions ✅
1.1 `cells/common/theme.nix`: the four `winter*` colours and roles `diffAdd = winterGreen`, `diffDelete = winterRed`, `diffChange = winterBlue`, `diffText = winterYellow`, `success = springGreen`, `warning = roninYellow`, `info = springBlue`. — DONE 2026-09, hexes re-checked against upstream master `lua/kanagawa/colors.lua` + wave `diff` in `themes.lua` (match).
Exit criteria: old palette JSON is a subset of the new one; commit `feat(theme): diff and status roles`.
verified 1: `nix eval --impure --json --expr 'removeAttrs (import ./cells/common/theme.nix {}) ["name"]'` before/after + recursive subset check -> true @ 2026-09; `alejandra --check cells/common/theme.nix` -> 0; consumers read only named keys (`k = theme.colors`/`r = theme.roles` aliases, no attrValues/mapAttrs over them) so elster drvPath should be unchanged; drvPath eval NOT run (age-plugin-tpm died, cold TPM cache) — user to run. No app changes, nothing to user-test.

## Phase 2 — bat + delta (+ lazygit diffs) ✅
2.1 DONE 2026-09 `cells/deck/homeModules/bat.nix`: `programs.bat.themes.kanagawa` (HM `bat.nix:15`, `{src, file}`) from `kanagawaSrc` `extras/tmTheme/kanagawa.tmTheme` (exists upstream, bg #1F1F28 / fg #DCD7BA / selection #2D4F67 = palette); `config.theme = "kanagawa"`. `kanagawaSrc` = fetchFromGitHub rebelot/kanagawa.nvim rev bb85e4bfc8d89b0e62c8fa53ccdd13d12e2f77b3 (master @ audit), let-bound in bat.nix (only consumer; btop 3.1 does not need it). HM rebuilds the bat cache in `home.activation.batCache` (HM `bat.nix:210`); skim's preview calls `${pkgs.bat}` (skim.nix:19), same config/cache, no change.
2.2 DONE 2026-09 `cells/deck/homeModules/git.nix:30` delta (25.05 `programs.git.delta.options`): `syntax-theme = "kanagawa"`; minus/plus(-emph) styles from `diff*` roles; line-number styles from `urgent`/`accent`/`muted`. lazygit's delta renderer (lazygit.nix:18) reads the same `[delta]` git config, no lazygit change for diffs.
2.2b DONE 2026-09 `cells/deck/homeModules/lazygit.nix`: `settings.gui.authorColors."*"` = a palette role, so author names stop being hash-generated truecolor (Matrix row "lazygit UI"). Chose `info` (springBlue).
2.3 User test (after rebuild): `git diff` and `git log -p` in a dirty repo; lazygit → a file with changes, staged + unstaged, commits panel (author colour); `bat` on a .nix and a .md file; `help git | head`; skim Ctrl-T preview. Look at: added/removed line bg, changed-word emphasis, line numbers, syntax colours vs nvim.
  — DONE 2026-09: user "всё ок", one finding: skim Ctrl-T preview of .md has no highlighting.
2.4 DONE 2026-09 Markdown in bat. Cause (measured): upstream kanagawa.tmTheme has no `markup.*` scopes except diff ones (no heading/bold/italic/raw/link/quote/list), so a README renders ~all #dcd7ba + #54546d gutter; not a skim problem, plain `bat README.md` is the same. Fix (user chose option 1 of 3: patch upstream, not regenerate/not exception): bat.nix `runCommand` appends 7 rules before `</array>` of upstream's tmTheme, colours from `theme.colors` per kanagawa.nvim wave treesitter links (heading → crystalBlue, raw → springGreen, link url → springBlue underline, quote → oniViolet2, list marker → springViolet1, bold/italic font styles). Palette addition `oniViolet2 = "b8b4d0"` (verbatim colors.lua:38).
  2.4 user test: skim Ctrl-T on a .md (README.md of the-hive) and `bat` on one: headings, `code`, links, `>` quotes, list markers, **bold**. Italic stays plain: bat's `--italic-text` defaults to never (same for upstream comments), not a theme issue. — DONE 2026-09: user "всё ок".
verified 2.4: standalone HM build of bat.nix (pins as above) -> tmTheme parses (`bat cache --build` okay); sample .md SGR: `#` + heading #7e9cd8, `-`/`1.` #938aa9, `>` quote #b8b4d0, `**b**` bold, `` `c` `` #98bb6c, url underline #7fb4ca; README.md 60 lines now 7 colours (was 2 fgs); bat.nix itself unchanged set @ 2026-09.
Exit criteria: `delta --show-config` shows `syntax-theme = kanagawa` and palette hexes; lazygit diff has no #3f0001/#002800; user accepted 2.3.
verified 2: `kanagawaSrc` hash via `nix flake prefetch github:rebelot/kanagawa.nvim/bb85e4b…` -> sha256-fMP4NUCK…; HM 44831a7 + nixpkgs 34ab999 standalone `homeManagerConfiguration` of bat/git/lazygit modules (/tmp, TPM-free) builds; generated `[delta]` = syntax-theme kanagawa, minus #43242b, plus #2b3328, emph #49443c, line numbers #c34043/#98bb6c/#727169; lazygit `gui.authorColors."*"` = #7fb4ca; in a throwaway HOME `bat cache --build` + `bat --list-themes` lists kanagawa, `delta --show-config` syntax-theme = Kanagawa, a real diff's SGR = only those bgs + tmTheme fgs, 0 hits of 3f0001/002800 @ 2026-09. tmTheme fgs outside our palette subset (still Kanagawa Wave upstream: #b8b4d0 oniViolet2, #9cabca springViolet2, #717c7c katanaGray) accepted as upstream, same set nvim gets in Phase 4. `alejandra --check` on the 3 files -> 0. elster drvPath NOT evaluated (cold TPM cache) — user.

## Phase 3 — btop from the palette ✅
3.1 `deck/homeModules/btop.nix`: `programs.btop.themes.kanagawa` (HM `btop.nix:63`, `lines`) from roles/colours; `color_theme = "kanagawa"` (replaces `kanagawa-wave` at btop.nix:14); drop the now-false comment btop.nix:11-12 ("no file to derive").
  — DONE 2026-09: gradient families kept from btop's bundled kanagawa-wave, off-palette hexes swapped (#dca561 → roninYellow/carpYellow, #957fbb → oniViolet, #7e9cdb → crystalBlue, #9cabca → oniViolet2); main_bg = roles.bg #1f1f28 (bundled used #16161d); temp/cpu/process = success→warning→urgent; added graph_text = muted, meter_bg = border (bundled omitted both).
3.2 User test: btop main view, then the menu (Esc) and the process filter (f). Look at: graph gradients, box borders, selected row. Known, not a theme bug: the menu's BTOP logo is red #cd2121 + greys #cc/#aa/#80, hardcoded (btop 1.4.7 src/btop.cpp:91, btop_menu.cpp:1222).
  — DONE 2026-09: user "ок".
verified 3.1: `alejandra --check btop.nix` -> 0; standalone HM (44831a7 + nixpkgs 34ab999, /tmp/btop-hm/eval.nix) builds, btop.conf `color_theme = "kanagawa"`, themes/kanagawa.theme 45 keys all palette hexes; private `tmux -L tc3` btop 1.4.7 with XDG_CONFIG_HOME=copy: main view truecolor = palette + gradient interpolations only, bg #1f1f28, selected row bg #2d4f67, no btop.log (no theme error); menu = palette + the hardcoded logo colours @ 2026-09. elster drvPath NOT evaluated (cold TPM cache) — user.
Exit criteria: `grep color_theme ~/.config/btop/btop.conf` == kanagawa, no fallback on start; user accepted 3.2.

## Phase 4 — neovim to kanagawa.nvim ✅
Audit 2026-09 (plan-audit, live code; paths under `dotfiles/nvim/lua/custom/` unless noted). Mechanism: nvim config is an out-of-store symlink (`cells/deck/homeModules/neovim.nix:38`), plugins come from lazy.nvim into the per-account persisted `~/.local/share/nvim` (layer-users-local.nix:90, auth-entra.nix:308), so no rebuild: edit lua, `:Lazy sync` on BOTH accounts.
4.1 Colorscheme spec + load sites. All 7 files with a catppuccin ref (`grep -rln catppuccin dotfiles/nvim`):
  - `plugins/colorscheme/init.lua:26-153`: replace the `catppuccin/nvim` spec with `rebelot/kanagawa.nvim`, `commit = "bb85e4bfc8d89b0e62c8fa53ccdd13d12e2f77b3"` (= bat.nix `kanagawaSrc`, one upstream rev for bat/delta/nvim), `theme = "wave"`, `transparent = constants.transparent_background` (was catppuccin opt :65). Keep today's look: `commentStyle`/`keywordStyle` = `{ italic = false }` (catppuccin had `no_italic = true` :70; kanagawa defaults both italic, init.lua:12,14 upstream), `statementStyle` bold stays. Drop the CATPPUCCIN_FLAVOR/ACCENT env parsing (:5-24; no setter anywhere in the repo) and the terminal_color autocmd (:32-57). Terminal colours: kanagawa's `terminalColors = true` writes its wave `term` table (themes.lua:174) = our `theme.ansi` except slot 0 (upstream sumiInk0 #16161d, ours sumiInk3 #1f1f28) + 2 extended slots — DECISION for the user (see Decisions).
  - `init.lua:42` `colorscheme "catppuccin-nvim"` → `"kanagawa-wave"` (upstream colors/kanagawa-wave.vim = `load "wave"`, sets `g:colors_name = "kanagawa"`, init.lua:58 upstream → exit criterion holds).
  - `lazy.lua:57` install fallback `{ "catppuccin", "habamax" }` → `{ "kanagawa", "habamax" }`.
  - `lazy-lock.json:14` catppuccin entry: goes away with `:Lazy clean`; kanagawa entry appears on `:Lazy sync` — commit the lockfile from the vkokurin side (the checkout is shared).
  - 7 `lazy_utils.on_load("catppuccin", …)` wrappers: ui.lua:193 (bufferline — its `setup()` is INSIDE the callback), ui.lua:682 (smear-cursor), ui.lua:702 (modes.nvim), ui.lua:726 (reactive.nvim, `enabled = false`), editor.lua:1664 (blame.nvim), editor.lua:1733 (sniprun), colorscheme/init.lua:35. Rename to `"kanagawa.nvim"` (the lazy spec name). If left, the callbacks never fire: no bufferline at all, smear/modes/blame/sniprun unconfigured. reactive.nvim is disabled and loads catppuccin-only presets: delete that spec (user's call, see Decisions) — else the exit grep fails.
4.2 Colours. Scope measured: hl_overrides.lua 465 lines, 191 `C.*` refs over 23 keys (counts: base 37, blue 24, text 21, mantle 18, red 16, teal 15, surface1 14, peach 8, overlay0 5, green 5, lavender 4, yellow/sun/sky/maroon/dark_purple 3, pink/mauve 2, vibrant_green/surface0/subtext0/sapphire/rosewater 1); outside it: ui.lua 6 refs (smear green, modes yellow/red/peach/green/teal/mauve), editor.lua 14 (blame: all 14 mocha accents as a rainbow list :1665-1680; sniprun: blue/red/base ×2), bufferline.lua 9 (base/surface1/lavender/green).
  Mechanism change vs the old text: NOT a rewrite of 191 refs into kanagawa `overrides(colors)`. hl_overrides is `C -> highlights` (keyed by flavour, :459-464), so feed it a catppuccin-shaped `C` built from `require("kanagawa.colors").setup().palette` via ONE key map, and pass `overrides = function() return hl(C) end`. All 191 refs stay byte-identical; the review surface is the 23-row map (+ blame's 14-colour list, which uses keys outside the 23: flamingo). Put the map in one module (e.g. `plugins/colorscheme/palette.lua`) that ui/editor/bufferline also require instead of `catppuccin.palettes`.
  Helpers: `catppuccin.utils.colors` is used 31× (hl_overrides darken 26 + brighten 3, editor.lua darken 2; not 4×): `darken(hex, a, bg)` = linear RGB `a*fg + (1-a)*bg` and `brighten` = HSLuv lightness lerp (catppuccin utils/colors.lua:24,36,40). kanagawa `lib.color` is NOT a drop-in: its blend is sqrt-RGB with the ratio reversed (lib/color.lua:35-56). Port the two functions into the existing `utils/colors.lua` (it already has hex↔rgb) and use `kanagawa.lib.hsluv` for brighten; outputs then equal today's math, only inputs change.
  Bufferline: `catppuccin.special.bufferline.get_theme` (bufferline.lua:8) supplies the base groups; kanagawa has no bufferline integration (0 matches in highlights/*.lua), so write the `custom.all` table as plain bufferline `highlights` (it already sets bg/fg for every group used).
  Plugin coverage lost with catppuccin's `integrations` (init.lua:98-151): kanagawa has groups for blink, gitsigns, neo-tree, neotest, dap, mini-indentscope, treesitter-context, trouble, window-picker; NONE for diffview, fidget, flash, grug-far, harpoon, markview, mason, noice, snacks, which-key, render-markdown, bufferline (grep of upstream highlights/*.lua). Those fall back to the plugins' own defaults (mostly links to standard groups) unless hl_overrides already sets them (it does for Diffview 6, Noice 11, Snacks 9, GrugFar 3, WhichKey 3, Flash 2). rainbow_delimiters is enabled but not installed (absent from lazy-lock) — nothing to port.
  Split: 4.2a map module + helpers + hl_overrides wiring; 4.2b ui/editor/bufferline onto the map.
  4.1+4.2 DONE 2026-09 (one step, the files overlap): colorscheme/init.lua = kanagawa spec (commit pinned, lazy=false, priority 1000, wave, italics off, overrides = hl_overrides(palette.get(colors.palette)), ColorScheme autocmd sets terminal_color_0 #1F1F28); new colorscheme/palette.lua = 23 keys + flamingo + the 3 old color_overrides keys (dark_purple/sun/vibrant_green) → wave names; utils/colors.lua gains blend/darken/brighten (catppuccin math, kanagawa.lib.hsluv); hl_overrides returns the function (no flavour table), `TreesitterContextBottom = {}` (was catppuccin-only `style`); 6 on_load renamed to "kanagawa.nvim", reactive.nvim spec deleted; bufferline highlights = plain table + `style_preset = {no_bold, no_italic}` (bufferline's own defaults are bold+italic, catppuccin stripped them); init.lua `kanagawa-wave`, lazy.lua fallback `kanagawa`; lazy-lock.json: catppuccin line → kanagawa.nvim @ bb85e4b (hand edit, no `:Lazy sync` so nothing else bumps).
  Unplanned: kanagawa paints gutter groups with `bg_gutter` = sumiInk4 (GitSigns*/DiagnosticSign* had a #2a2a37 strip); set `colors.theme.all.ui.bg_gutter = "none"` so signs sit on the editor bg as before.
  Map choices (review surface): base sumiInk3, mantle sumiInk1, surface0 sumiInk4, surface1 sumiInk6, overlay0 fujiGray, subtext0/rosewater oldWhite, text fujiWhite, flamingo oniViolet2, pink sakuraPink, mauve oniViolet, red waveRed, maroon peachRed, peach surimiOrange, yellow/sun carpYellow, green/vibrant_green springGreen, teal waveAqua2, sky springBlue, sapphire lightBlue, blue crystalBlue, lavender springViolet2, dark_purple springViolet1.
4.3 DONE 2026-09, user "ок!". User test (after `:Lazy sync` + `:Lazy clean`, BOTH accounts): a .nix and a .lua file, snacks picker, completion popup (blink), diagnostics (introduce an error), git signs, bufferline with 3 buffers (it must exist at all — on_load rename), modes.nvim line colours (insert/visual/delete), smear cursor colour, `:terminal` + `printf '\e[40m  \e[0m'` (slot 0), a diff (`:Gitsigns diffthis` or diffview), blame.nvim, noice cmdline + a notification. Look at: overrides that used catppuccin colours, floating window borders, terminal colours, comments/keywords NOT italic.
verified 4.1-4.2: isolated XDG_DATA/STATE/CACHE in /tmp/nvtest (copy of vkokurin's lazy/ + kanagawa.nvim @ bb85e4b; real ~/.local/share/nvim untouched): colors_name -> kanagawa; `nvim --headless +qa` -> empty, rc 0; `grep -rn catppuccin dotfiles/nvim` -> empty; `:messages` empty after loading bufferline/modes/blame/sniprun + VeryLazy; Normal #dcd7ba/#1f1f28, Comment/Keyword not italic, FloatBorder #7e9cd8, DiffAdd #32392f, SignColumn/LineNr bg #1f1f28, GitSignsAdd bg none, BufferLineBufferSelected #9cabca no bold/italic, tabline = nvim_bufferline(); utils.colors darken+brighten == catppuccin utils on 4 samples (0 mismatches); `stylua --check` clean on touched files (pre-existing diffs in macros/lang-haskell/lang-web/lsp/statusline untouched) @ 2026-09.
Exit criteria: `nvim --headless -c 'lua print(vim.g.colors_name)' -c qa` == kanagawa; `grep -rn catppuccin dotfiles/nvim` empty; `nvim --headless +qa 2>&1` empty; user accepted 4.3 on both accounts.
Decisions owed before 4.1 (ask at the start of the Phase 4 session, one at a time):
  - terminal slot 0: (a) accept upstream sumiInk0 in `:terminal`, (b) override `vim.g.terminal_color_0` to #1f1f28 in the kanagawa spec (one literal in lua; nvim cannot read theme.nix), (c) `terminalColors = false` (nvim then falls back to its builtin terminal defaults, not foot's — worse).
  - reactive.nvim (disabled): delete the spec, or keep it and rewrite its presets.
  - blame.nvim rainbow: 14 mocha accents → the closest 14 kanagawa accents, or a shorter list.
  Answered 2026-09: slot 0 = (b) `vim.g.terminal_color_0 = "#1f1f28"`; reactive.nvim spec deleted; blame = 14 accents through the same map (+ flamingo).

## Phase 5 — Standalone GUI/TUI configs ✅
Audit 2026-09 (plan-audit, live code + HM 44831a7 = `home-manager_3`, the root `home-manager` input in cells/workstation/flake.lock). Palette access: desktop modules take the `theme` arg (home/default.nix:50 `_module.args.theme`, e.g. qt.nix:9); deck modules read `inputs.cells.common.theme` (btop.nix:8).
5.1 Zathura: bare package at `home/dev/languages.nix:50` (forward-search for `lang-texlive.lua:56`). Move to `programs.zathura.enable` (HM `zathura.nix:26`, installs `cfg.package` at :84, writes zathurarc at :86) + `programs.zathura.options` from roles: default-bg/fg, statusbar-bg/fg, inputbar-bg/fg, notification(-error/-warning)-bg/fg, highlight-color/-active-color, completion-*, index-*, recolor-lightcolor/-darkcolor; `recolor` stays false. Where: a new `home/desktop/zathura.nix` (languages.nix has no `theme` arg today; dev/ is imported alongside desktop/ by home/default.nix:62, so either works — desktop keeps GUI theming together). Both accounts get it (same home module, built twice: home/default.nix:1-5).
  — DONE 2026-09: new `home/desktop/zathura.nix` (imported in desktop/default.nix), bare `zathura` dropped from languages.nix. Options per zathurarc(5) of the installed 2026.05.20: plan's set + completion-group-*, highlight-fg, render-loading-bg/fg, signature-{success,warning,error}-color. Overlays over the page use `rgba()` (GTK3 parses no #rrggbbaa): highlight = highlight@0.5, active = warning@0.5, signatures @0.9. recolor-lightcolor = bg, -darkcolor = fg.
5.2 mpv: `osd-color`, `osd-border-color`, `osd-back-color`, `osd-selected-color` (all in mpv 0.41 man) from roles in `programs.mpv.config` (mpv/default.nix:33). These colour OSD text + the `o`/seek OSD bar only. The OSC (seekbar/controls on mouse move) is osc.lua with its own `background_color`, `timecode_color`, `title_color`, `buttons_color`, `held_element_color` script-opts → `programs.mpv.scriptOpts.osc` (HM mpv.nix:95). Include both, else the most-seen mpv UI stays default.
  — DONE 2026-09: `osd-color` fg, `osd-outline-color` (= border alias) bgDim, `osd-back-color` #AF+bg (mpv's default alpha 175), `osd-selected-color` highlight, `osd-selected-outline-color` bgDim; `scriptOpts.osc` = all 10 colour keys of mpv 0.41 osc.lua :58-68 (bg, fg for text/buttons, held = focus, outline bgDim). osc.lua accepts `#rrggbb` only (:2940 warns otherwise) — all values are.
5.3 avizo: `services.avizo.settings.default` (HM `services/avizo.nix:17`, ini at :54) in `home/desktop/osd.nix:6`: `background`, `border-color`, `bar-fg-color`, `bar-bg-color` (keys from avizo_client.vala / upstream config.ini). Parsed by Gdk.RGBA (`#rrggbb` or `rgba(...)`); a bad value `error()`s and kills the client (avizo_client.vala:285), so eval-check the rendered ini. Read by the client (volumectl/lightctl) on every call, so no service restart needed.
  — DONE 2026-09: `services.avizo.settings.default` background bg@0.95, border focus, bar-fg fg, bar-bg bgAlt (rgba() from hex). Unplanned: avizo icons are fixed PNGs, default set is black (#000000, invisible on #1f1f28); `-d` picks the grey #a0a0a0 set. Added `-d` to every volumectl/lightctl call: packages/dwl/config.h:186-194 (XF86 keys) and home/desktop/bar.nix:205-216 (waybar scroll/click). dwl rebuilds.
5.4 ANSI TUIs with their own colours (Phase 0):
  - nmtui: `NEWT_COLORS` (libnewt 0.52.24 reads NEWT_COLORS / NEWT_COLORS_FILE) via `home.sessionVariables` (pattern: direnv.nix:17). Values are newt colour NAMES (black, red, …, brightblue), i.e. ANSI slots → foot palette; no hexes. Elements: root, border, window, shadow, title, button, actbutton, checkbox, actcheckbox, entry, label, listbox, actlistbox, textbox, acttextbox, helpline, roottext, emptyscale, fullscale, disentry, compactbutton, sellistbox, actsellistbox.
  - fzf: `programs.fzf.colors` (HM `fzf.nix:152`, appended to FZF_DEFAULT_OPTS at :205). Low value: no config in the repo invokes fzf (widgets are skim, nvim uses snacks); still themed for ad-hoc use.
  - skim: installed sk is 5.4.0 (ratatui rewrite), not 0.10. `--color` = optional base theme then `name:#rrggbb[:mods]`; names normal/fg, bg, matched/hl, current/fg+, bg+, current_match/hl+, query, spinner, info, prompt, cursor/pointer, selected/marker, header, border, scrollbar (skim v5.4.0 src/theme.rs:285-360). No `colors` option on this HM pin → `--color=…` in `programs.skim.defaultOptions` (→ SKIM_DEFAULT_OPTIONS, HM skim.nix:109; Ctrl-T/Alt-C prepend it, skim key-bindings.zsh:60,103).
  - Ctrl-R (zsh-histdb-skim 0.9.7) does NOT read SKIM_DEFAULT_OPTIONS: it links skim 0.10.4 as a library and passes `.color(HISTDB_COLOR)` (src/environment.rs:78, default "auto"). Set `HISTDB_COLOR` session var to the same string restricted to names both parsers accept (fg, bg, hl, fg+, bg+, hl+, info, prompt, pointer/cursor, marker/selected, spinner, header, border; 0.10's theme.rs takes `#rrggbb` too). One Nix string, two env vars.
  - p10k: only `POWERLEVEL9K_PERLBREW_FOREGROUND=67` (.p10k.zsh:1186) is a cube colour; 208 was a commented example (:1682), rust_version's built-in darkorange is already overridden to 4 (:1066). One edit → an ANSI index (6 or 4). man/less already ANSI-only, no change.
  — DONE 2026-09: nmtui → new deck module `homeModules/newt.nix` (all 23 elements, newt.c:249-294 of 0.52.24; bg = black slot, accents = blue, fields = lightgray bg), registered in deck/homeModules.nix (reaches both homes via `builtins.attrValues deck`). skim.nix: one `color` string from roles, base `empty` (both 0.10.4 and 5.4 parse it; 5.4 skips `fg`/`current_match_bg`, 0.10 skips `normal`/`scrollbar`, both silently), → `defaultOptions = ["--color=…"]` and `home.sessionVariables.HISTDB_COLOR`. fzf.nix: `programs.fzf.colors` (fzf 0.74.3 names incl. gutter). p10k: PERLBREW 67 → 6 (= PLENV_FOREGROUND :1173).
5.5 KeePassXC: `[GUI] ApplicationTheme=classic` (Config.cpp:117, default "auto"; "auto" + prefer-dark → its own DarkStyle, Application.cpp:167-181; "classic" = no style/palette override, only classicstyle.qss built from `palette(...)` refs → takes the qt5ct palette). NOT via HM `programs.keepassxc.settings` (HM keepassxc.nix:53 → read-only store symlink). Correction: LastDatabases/LastOpenedDatabases/geometry are `Local` settings in ~/.cache/keepassxc/keepassxc.ini (Config.cpp:91,95,127,564), not in the roaming ~/.config one — the symlink would still block KeePassXC's own settings writes (it saves the whole roaming ini), so the activation-edit approach stands. Today ~/.config/keepassxc/keepassxc.ini (vkokurin) has only [General]/[PasswordGenerator]; the edit adds `[GUI] ApplicationTheme=classic` idempotently (both accounts; persisted at layer-users-local.nix:118, auth-entra.nix:329; the Entra home is a standalone activationPackage, activation still runs). keepass.nix:7-10 comment "Recent-db list is under .cache" (layer-users-local.nix:117) already matches.
  Vial: the plan's `QT_STYLE_OVERRIDE`/palette env CANNOT work. Vial 0.7.5 calls `QApplication.setPalette(<hardcoded Dark>)` + Fusion itself at startup (vial-gui v0.7.5 themes.py:27,248; default theme "Dark", main_window.py:417). The only non-hardcoded path is its "System" theme (no setPalette) stored in `QSettings("Vial","Vial")` = ~/.config/Vial/Vial.conf, which is NOT persisted on either account (grep of both impermanence lists: no Vial) → erased at boot. Bundled Qt is 5.9.3 with only `libqgtk3` as platformtheme (no qt5ct, cannot load the system 5.15 plugin). So: theme=System + `QT_QPA_PLATFORMTHEME=gtk3` in the FHS wrapper, might pick up GTK Kanagawa (unverified: the bundled qgtk3 must find a GTK3 in the FHS env); needs either persisting ~/.config/Vial or writing Vial.conf in the wrapper. DECISION for the user (see Decisions), fallback = exception.
  — DONE 2026-09: keepass.nix `home.activation.keepassxcTheme` = crudini `--set GUI ApplicationTheme classic` (same pattern as torrent.nix:67), both accounts. Checked against 2.7.12 Application.cpp:166-199: classic = no own style, classicstyle.qss, dark icons still from isDarkMode.
5.6 User test, one line per app: Zathura (open a PDF, `/` search → highlight, statusbar, index `Tab`, `Ctrl+R` recolor); mpv (move the mouse → OSC bar/timecode; seek → OSD bar; `o`; pause text); avizo (volume and brightness keys); nmtui (main menu + an edit dialog); skim Ctrl-T, Alt-C and Ctrl-R (histdb); fzf (`fd | fzf` ad hoc); p10k prompt in a git repo (perlbrew segment not observable without perlbrew — skip); KeePassXC (main window, entry edit dialog, settings; reopen: last DB still remembered). avizo: icons are grey now (-d), both keys and waybar scroll.
  — DONE 2026-09: user "всё ок". First fzf/Ctrl-R look was stale: panes started before the rebuild, and `exec zsh` inherits `__HM_SESS_VARS_SOURCED=1` so hm-session-vars.sh is skipped; a new tmux pane picked the vars up. Test session vars in a NEW pane, not `exec zsh`.
Decisions owed before 5.5 (ask at the start of the Phase 5 session):
  - Vial: (a) exception (hardcoded palette, 5 min/year use), (b) try theme=System + QT_QPA_PLATFORMTHEME=gtk3 in the wrapper and persist ~/.config/Vial on both accounts, (c) same but the wrapper seeds Vial.conf each launch (no persistence entry).
  Answered 2026-09: (a) exception. 5.5 = KeePassXC only; Vial dropped from 5.6.
verified 5.2-5.5: standalone HM eval (/tmp/zat-hm/p5.nix: zathura, mpv, osd, keepass + deck skim/fzf/newt, stub `inputs.cells.common.theme`) -> mpv.conf/osc.conf/avizo ini/4 env vars/activation rendered, home-files builds; `alejandra --check` on all touched .nix -> 0; `zsh -n .p10k.zsh` -> 0. Live, headless dwl (pixman) + private `tmux -L tc5` @ 2026-09:
  - mpv 0.41 (--config-dir=rendered, vo=wlshm) on a #808080 clip: OSD text + show-progress bar fg #dcd7ba, outline #16161d; OSC box = bg blended by boxalpha, text/buttons fg; mpv.log no "not a valid color".
  - avizo-service on a private dbus + XDG_CONFIG_HOME=rendered: bg #1f1f28, border #7e9cd8, bar #dcd7ba/#2a2a37; `volume_high` icon #000000 vs `_dark` #a0a0a0 (-> the -d change).
  - KeePassXC 2.7.12, throwaway HOME + user's qt6ct: classic -> #1f1f28/#16161d/#dcd7ba; auto (today) -> #eff0f1 (its own light style under a headless session).
  - SGR: sk 5.4 = palette truecolor only (fg, highlight, accent, hover, selection bg, border, muted, focus); fzf same; zsh-histdb-skim bg #1f1f28, border #363646, header #7fb4ca, prompt #7e9cd8, info #727169; nmtui main + edit list = only 30/34/97/40/44 (ANSI slots, i.e. foot).
  elster drvPath NOT evaluated (cold TPM cache) — user.
verified 5.1: standalone HM eval (HM y3s3hn0…-source + workstation nixpkgs fmasn49…-source, /tmp/zat-hm/eval.nix) -> zathurarc 32 `set` lines, all palette hexes / rgba of palette; `alejandra --check` on the 3 files -> 0; headless dwl (pixman) + `zathura -c <rendered cfg>` on a PDF: surround #1f1f28, statusbar #2a2a37, inputbar ok, `--find` highlight blends of #e6c384/#ff9e3b on white, index (Tab) bg #1f1f28 / selected #2d4f67 / fg #dcd7ba, Ctrl+R page #1f1f28 @ 2026-09. elster drvPath NOT evaluated (cold TPM cache) — user.
Exit criteria: screenshot of each tool shows palette bg/fg; user accepted 5.6 per app; Matrix rows flipped to themed.

## Phase 6 — Agent CLIs ✅
Audit 2026-09 (plan-audit, live binaries: hermes 0.20.5 = `/nix/store/wr8avsva…-hermes-agent-2026.8.19`, claude-code 2.1.239 `.claude-wrapped` strings, opencode 1.18.21 `.opencode-wrapped` strings). Not started: no ~/.hermes/skins, no ~/.claude/themes, `display.skin` = default, tui.json theme "opencode" (agents.nix:126). Palette access: agents.nix takes `{config, pkgs, lib, agentPkgs, ...}` (:22-28) — add `theme` (home/default.nix:50 `_module.args.theme`); agent-proxy.nix has `inputs` (:23) → `inputs.cells.common.theme`. Both homes get agents.nix (home/default.nix:62 imports ./dev), hermes is system-wide (agent-proxy.nix:104) + a per-user seed unit (:141), so every item below reaches both accounts without extra wiring.
6.1 Hermes skin. Schema = skin_engine.py:13-111; loader: `load_skin` reads `<HERMES_HOME>/skins/<name>.yaml` (`is_file()` follows a store symlink, :897), yaml needs a `name` key (:799), missing keys inherit `default` (:831), unknown name → silent fallback to default with a log warning (:907). Colour keys actually read (grep `get_color(` over the package): banner_{border,title,accent,dim,text}, ui_{accent,label,ok,error,warn}, prompt, input_rule, response_border, session_{label,border}, status_bar_{bg,text,strong,dim,good,warn,bad,critical}, completion_menu_{,current_,meta_,meta_current_}bg, voice_status_bg (+ the TUI-only background/selection_bg/diff_*/syntax_* documented at :26-68, cheap to set too). Values `#rrggbb` only (display.py:64 and cli.py `_hex_to_ansi` slice [1:3]…).
  Where: NOT HM `home.file` as the old text said — hermes has no HM presence (agent-proxy.nix:7-13, system-wide + `seedHermes`). That comment's "Entra user … no home-manager" is stale (home-manager-entra exists, layer-compositor.nix:326), but HM home.file would still split ~/.hermes wiring across two files for no gain. Put the skin as a store file in agent-proxy.nix and `ln -sfn` it to `~/.hermes/skins/kanagawa.yaml` inside `seedHermes` (same as the rtk plugin link, :92-96), plus `display.skin kanagawa` via the get-compare-set loop (:74-79). Keeps all ~/.hermes wiring in one file. skins/ is per-home (not one of the /srv/agents links, :84) — right. Side effect: a `/skin <x>` choice is reset on the next login, same as `model.*` today.
  Hardcoded, NOT skinnable (measured): (a) banner logo + caduceus are Rich-markup art with literal #FFD700/#FFBF00/#CD7F32… (banner.py:70,77; cli.py:4680,4688), replaced only if the skin sets `banner_logo`/`banner_hero` (banner.py:983,1265) — DECISION; (b) diff line bgs are computed, not read: minus = `(r//2, g//4, b//4)` of ui_error, plus = `(r//4, g//2, b//4)` of ui_ok, fg forced #ffffff (display.py:74-82) — ui_error autumnRed → #611010, ui_ok springGreen → #265d1b; winterRed/winterGreen are unreachable without patching hermes → known off-palette, like btop's logo; (c) `_GOLD` #FFD700 fallback in banner.py:33 / cli.py:2926 only when the skin lookup throws.
6.2 Claude Code. Theme file loader (2.1.239 strings): `<config dir>/themes/*.json` (readdir, `.json` only, ≤256KB, stat/readFile follow a symlink), slug = basename; `{name, base, overrides}`; `base` ∈ dark|light|light-daltonized|dark-daltonized|light-ansi|dark-ansi (else "dark"); an override is kept ONLY if the key exists in the base palette (`Object.hasOwn(she(base), key)`) and the value is `#rgb`/`#rrggbb`/`rgb(r,g,b)`/`ansi256(n)`/`ansi:<name>` — anything else is dropped SILENTLY, so verify by count. Config dir = `$CLAUDE_CONFIG_DIR` = `$HOME/.claude` (layer-session.nix:153; `.claude` persisted: layer-users-local.nix:78, auth-entra.nix:291). Setting: schema `theme: … O().startsWith("custom:")`; effective theme = settings.json `theme` ?? ~/.claude.json `theme` (`s5t`: `t.theme??e.theme`) → `theme = "custom:kanagawa"` in `claudeSettings` (agents.nix:59) wins over whatever `/theme` writes. `.claude/themes/kanagawa.json` via `home.file` next to settings.json (agents.nix:144).
  Key set = the dark base (`bGv`), 70 keys: autoAccept(+Shimmer), skill, bashBorder, claude(+Shimmer), claudeBlue_FOR_SYSTEM_SPINNER(+Shimmer), permission(+Shimmer), planMode, ide, promptBorder(+Shimmer), text, inverseText, inactive(+Shimmer), subtle, suggestion, remember, background, success, error, warning(+Shimmer), merged, diffAdded, diffRemoved, diffAddedDimmed, diffRemovedDimmed, diffAddedWord, diffRemovedWord, {red,blue,green,yellow,purple,orange,pink,cyan}_FOR_SUBAGENTS_ONLY, professionalBlue, chromeYellow, clawd_body, clawd_background, userMessageBackground(+Hover), composerSidebarBackground, selectionBg, bashMessageBackgroundColor, memoryBackgroundColor, rate_limit_fill/empty, fastMode(+Shimmer), effortUltra, briefLabelYou/Claude, rainbow_{7}(+_shimmer). Map all of them (unset keys keep Claude's defaults). Note `background` in Claude's palette is a cyan accent, not the screen bg. `diffAddedWord`/`diffRemovedWord` are used both as word-level bg in diffs AND as fg of the `+N`/`-N` counts, so they must read as a fg on #1f1f28 (not winterYellow).
  6.2 DONE 2026-09 agents.nix: `theme` arg; `claudeSettings.theme = "custom:kanagawa"`; `claudeTheme` {name Kanagawa, base dark, 72 overrides from roles/colours} → `home.file.".claude/themes/kanagawa.json"`. Correction: the dark base has 72 keys, not 70 (audit miscount). Map: text fg, subtle/promptBorder sumiInk6, blues (permission/suggestion/spinner/rate_limit) focus + info shimmer, `background` (cyan accent) waveAqua2, planMode waveAqua1, violets (autoAccept/skill/merged/effortUltra) hover, bashBorder sakuraPink, claude/clawd orange surimiOrange, diff line bgs (+Dimmed) diffAdd/diffDelete, diffAddedWord autumnGreen / diffRemovedWord urgent (word bg + `+N/-N` fg), message bgs bgAlt/border/sumiInk1/waveBlue1, selectionBg selection, subagent + rainbow = nearest wave accents.
  verified 6.2: standalone build /tmp/cct/eval.nix (theme.nix imported directly, TPM-free) -> theme json + settings.json `theme` = custom:kanagawa; `comm` of our keys vs the dark palette object in `.claude-wrapped` strings -> 72 = 72, 0 extra, 0 missing; 29 distinct values, all `#rrggbb` in theme.nix, 0 off-palette; `alejandra --check agents.nix` -> 0 @ 2026-09. Live SGR histogram + `/theme` listing wait for the rebuild (6.4). elster drvPath NOT evaluated (cold TPM cache) — user.
6.3 opencode. Old text "built-in kanagawa" is NOT palette-exact (strings of the built-in): carpYellow = #C38D9D (not e6c384; used by borderActive + markdownEmph + syntaxType), roninYellow = #D7A657 (not ff9e3b), and 7 literal diff hexes off-palette (#A9D977, #F24A4A, #252E25, #362020, #9090a0, #202820, #2D1C1C); background = #1F1F28 is ok. Custom themes: `discover()` scans `themes/*.json` under `Path.config` (= ~/.config/opencode) and each `.opencode/` up from cwd, symlinks followed; merge order `{...builtin, ...plugin, ...custom}` → a user `kanagawa.json` shadows the built-in. Needs a `theme` object (validator `K`), values = `#hex` | defs name | theme-key ref | `{dark, light}` | `transparent`/`none`. So: `xdg.configFile."opencode/themes/kanagawa.json"` generated from roles with the built-in's 48 keys (primary…syntaxPunctuation, list in this audit's grep), `opencodeTui.theme = "kanagawa"` (agents.nix:126). tui.json `theme` beats the KV store (`d.theme ?? t.get("theme")`).
  6.3 DONE 2026-09 agents.nix: `opencodeTheme` → `xdg.configFile."opencode/themes/kanagawa.json"`, `opencodeTui.theme = "kanagawa"`. Correction: the built-in has 50 theme keys, not 48 (optional `selectedListItemText`/`backgroundMenu`/`thinkingOpacity` left unset: they default to background/backgroundElement/0.6). `diffHighlight*` are the `+`/`-` sign fg (`addedSignColor`), not word bgs → success/urgent. Map: primary focus, secondary hover, accent sakuraPink, error urgent (built-in dragonRed), warning roninYellow, panels bgAlt/border, borderActive focus (built-in off-palette #C38D9D), diff bgs diffAdd/diffDelete (+ line-number bgs), markdown/syntax per kanagawa.nvim wave (heading crystalBlue, code springGreen, quote oniViolet2, list springViolet1, keyword oniViolet, type waveAqua2, number sakuraPink, operator boatYellow2, punctuation springViolet1).
  verified 6.3: standalone build /tmp/oct/eval.nix (theme.nix direct, TPM-free) -> tui.json theme kanagawa; `comm` of our keys vs the built-in kanagawa in `.opencode-wrapped` strings -> 50 = 50, 0 missing, 0 extra; 20 distinct values, 0 off-palette; `alejandra --check agents.nix` -> 0; private `tmux -L tc63` opencode 1.18.21, throwaway XDG dirs: stderr empty, `ctrl+p theme → Switch theme` lists `● kanagawa`, home screen truecolor = #1f1f28 #727169 #dcd7ba #957fb8 #363646 #ff9e3b (our warning; built-in's is #D7A657 → the custom file shadows it) + hardcoded logo shading #4e4d4d/#343438 (▀ glyphs only) + #ffffff on blank cells only; dialog backdrop = palette dimmed by opencode's overlay alpha (#0d0d10 = #1f1f28 × ~0.42) @ 2026-09. Session view/diff/markdown need a provider → 6.4. elster drvPath NOT evaluated (cold TPM cache) — user.
6.4 User test (after rebuild; for hermes also a re-login or `systemctl --user restart hermes-seed-proxy`, it is a oneshot): hermes (banner, a tool call, a patch diff, a clarify prompt, completion menu `/`, status bar); claude (prompt, tool call, a diff with word highlight, permission dialog, a subagent, `/theme` lists kanagawa); opencode (session view, diff, command palette, markdown answer). Both accounts.
  — DONE 2026-09: user "ок по каждому". pt chrome stays 256-colour (option (a) kept, no patch).
verified 6.4: after rebuild, live links: ~/.claude/themes/kanagawa.json + ~/.config/opencode/themes/kanagawa.json → home-manager-files, ~/.hermes/skins/kanagawa.yaml → hermes-skin-kanagawa.yaml; settings.json theme custom:kanagawa, tui.json theme kanagawa, `hermes config get display.skin` -> kanagawa @ 2026-09. Private-tmux SGR of claude returned no truecolor escapes (not investigated; user acceptance stands).
Exit criteria: per tool a private-tmux SGR histogram (Phase 5 method) = palette truecolor only, except the listed hardcoded ones (hermes diff bgs); claude theme file: every override key accepted (count == keys written); user accepted 6.4 per tool; Matrix rows flipped. (Old "bg #1f1f28 in a screenshot" is vacuous for hermes/claude — both leave bg to foot.)
Decisions owed before 6.1 (ask one at a time at the start of the Phase 6 session):
  - hermes banner art: (a) generate `banner_logo`/`banner_hero` at build time from the pinned hermes banner.py with the gold/bronze hexes swapped for roles (follows a hermes bump automatically, fails the build if the constants move), (b) leave the art gold, record as known off-palette, (c) `display.compact true` (cli.py:490, 5014; `_build_compact_banner` :4706 is text-only and takes banner_border/title/dim from the skin — no art, fully palette; also used automatically below 80 cols, :8375).
  - opencode: (a) own kanagawa.json from roles (exact palette), (b) keep the built-in (off-palette yellows/diffs, accept as "upstream"), (c) `theme = "system"` (derives from the terminal's 16 colours via an OSC query; falls back to "opencode" when the palette query returns nothing; whether tmux 3.x answers it here is unverified).
  Answered 2026-09: banner = (c). 6.1 adds `display.compact true` to the seedHermes get-compare-set (next to `display.skin`); `compact` is read only at cli.py:5014 and used only for the banner (:8375 startup, :11876 redraw), nothing else changes. Banner art drops out of the hardcoded list; 6.4 checks the compact banner's border/title/dim.
  Answered 2026-09: opencode = (a) own `opencode/themes/kanagawa.json` from roles (48 keys), per 6.3.
  6.1 (in progress 2026-09) agent-proxy.nix: `pinned` = model.* + `display.skin kanagawa` through the one get-compare-set loop; `hermesSkin` = 41 colour keys from roles (every key the default skin + get_color() read) + `banner_logo`/`banner_hero` generated by `profiles/__hermes-skin-art.py` from the pinned banner.py (ast-read constants, gold ladder FFD700/FFBF00/CD7F32/B8860B → lightBlue/springBlue/crystalBlue/springViolet1; unmapped hex or missing constant fails the build); seed links it to ~/.hermes/skins/kanagawa.yaml.
    Decision REVISED: `display.compact` does nothing in 0.20.5: hermes_cli/main.py:3256 always passes `compact=False`, so cli.py:5014's `CLI_CONFIG` fallback never runs (`is not None`). Measured: full gold banner with compact: true. User chose (a) 2026-09: skin art at build time; compact dropped from `pinned`.
    NEW finding: hermes' prompt_toolkit chrome (status bar, completion menu, input rule, clarify/approval panels) is quantised to 256 colours. cli.py:4464 builds `Vt100_Output(stdout, …, enable_cpr=False)` directly (always on POSIX, :4428), bypassing `create_output`, so `PROMPT_TOOLKIT_COLOR_DEPTH` is never read and pt 3.0.52 vt100.py:760 falls back to DEPTH_8_BIT. Measured with PROMPT_TOOLKIT_COLOR_DEPTH=DEPTH_24_BIT: still 38;5;187/242, 48;5;236/238/248. Rich output (banner, responses) is truecolor. DECISION owed.
    Options: (a) accept as known off-palette (skin colours, cube-256 nearest at the terminal) + upstream issue; (b) postPatch `default_color_depth=ColorDepth.DEPTH_24_BIT` into cli.py `_build_cpr_disabled_output` via repo/packages.nix + upstream PR; (c) chrome = exception. No answer in the session (clarify timed out) → (a) taken by Claude as the reversible default; user may flip to (b) at 6.4.
  6.1 DONE 2026-09 (code; user test batched into 6.4).
  verified 6.1: standalone build /tmp/hskin/eval.nix (stub theme input, real `.#x86_64-linux.repo.packages.hermes-agent`) -> seed script with 4 get-compare-set blocks + skin link; skin yaml hexes all in theme.colors; skin_engine.load_skin("kanagawa") under a throwaway HERMES_HOME: 41 keys, 0 default keys left, 0 off-palette; private `tmux -L tc6` startup banner truecolor = #7e9cd8 #727169 #dcd7ba #7fb4ca #938aa9 #a3d4d5 #98bb6c #e6c384 only (was #ffbf00/#cd7f32/#b8860b/#ffd700); `alejandra --check agent-proxy.nix` -> 0 @ 2026-09.

## Phase 7 — Browsers, Electron, PWAs, Horizon ⏳
Audit 2026-09 (plan-audit, live code + installed binaries: Firefox 154.0.1 `firefox-unwrapped-154.0.1`, Edge 152.0.4191.53, teams-for-linux 2.17.1 `app.asar`, rust_o365 4.0.0, Dark Reader 4.9.133 xpi from the vkokurin profile, omnissa-horizon-files-2605, nixpkgs `fmasn49…-source`). Nothing started (`git log` on the touched files: no theme commits). Account split (layer-compositor.nix:53-64): Slack, Telegram, Grayjay, Horizon = Entra home only; Firefox, Edge (+ Dark Reader), Mattermost, SimpleX = both accounts; o365 = Entra only (auth-entra.nix:542).
Split by mechanism, because most of this phase is NOT declarative: server-side or in-app state (Mattermost, Slack, Telegram, Dark Reader) can only get a palette-generated artefact the user imports once per account; Nix can own only Horizon, teams-for-linux CSS, and browser chrome.
7.1 Firefox chrome. Old text "follow GTK dark or userChrome/Edge theme" was unmeasured; facts:
  - Profile has `extensions.activeThemeID` = the built-in default-theme id (= "System theme — auto"); whether Fx 154 paints its toolbar from our GTK Kanagawa colours in dark mode is UNMEASURED → first step: headless dwl + throwaway profile (Phase 0.2 method), screenshot tabs/urlbar/menu, pixel-compare to #1f1f28/#16161d/#2a2a37.
  - Own palette static theme (manifest `theme.colors` from roles) CANNOT be installed: nixpkgs builds with `requireSigning ? true` (build-mozilla-mach/default.nix:11), release branding ignores `xpinstall.signatures.required`. AMO has only third-party Kanagawa themes, e.g. `kanagawa-wave-dark-theme` 2.5 = off-palette toolbar #313140, carpYellow tab text.
  - userChrome.css is already possible: `toolkit.legacyUserProfileCustomizations.stylesheets = true` (browser-firefox.nix:153). But the profile is Firefox-owned (random `ubw2338r.default` under ~/.config/mozilla/firefox, no HM programs.firefox), so the file needs a path into a profile whose name Nix does not know (activation glob over profiles.ini, per account).
  - Dark Reader `changeBrowserTheme` (default false, background/index.js ~:6270) repaints chrome via `browser.theme.update` with its own modify*Color of fixed greys — derived, not palette-exact.
  - Invariant: do NOT flip `layout.css.prefers-color-scheme.content-override` (Phoenix phoenix.cfg:2427 sets 1 = light on purpose, fingerprinting); content darkness stays Dark Reader's job.
  DECISION (below).
  7.1a measured 2026-09: headless dwl (pixman) + throwaway `--profile` (system policies/addons applied, activeThemeID default = System theme auto), real HOME GTK settings (Kanagawa, prefer-dark), histogram of `grim`: tabstrip + inactive tabs #2a2a37 (= bgAlt, from GTK headerbar), tab text #dcd7ba, accent #7e9cd8; active tab, navbar, bookmarks bar #414148, urlbar field #2d2d32, borders #444444 — the last three are Firefox's own blends, not in the palette. → row `partial`, not `inherits`. Throwaway profile + dwl removed; screenshot /tmp/ff71-chrome.png.
  DECISION owed (7.1b): (b) userChrome.css from roles, linked into every profile by an activation glob over profiles.ini; (c) Dark Reader `changeBrowserTheme` (derived greys); (d) accept partial as exception.
  Answered 2026-09: (b).
  7.1b DONE 2026-09: home/desktop/firefox.nix: userChrome.css = `:root` design-system tokens of Fx 154 from roles (42 tokens, names taken from the installed omni.ja, each has `var(--x)` readers; old `--toolbar-bgcolor`/`--lwt-*`/`--arrowpanel-*` names are gone in 154), `!important`; HM activation links it into `chrome/` of every `Path=` in profiles.ini, skips a hand-written regular file. Both accounts (desktop/ is shared).
  verified 7.1b: activation run against a throwaway profiles.ini -> symlink into abc.default/chrome; headless dwl + that profile: tabstrip #2a2a37, active tab/navbar/bookmarks #1f1f28, urlbar field #16161d, urlbar pill #2a2a37 (was #2f2f31 = fg 16% alpha, fixed by urlbar-box-*), text #dcd7ba, accent #7e9cd8 — 0 off-palette in the top 3 per region; menus/panels not opened headless (user test). `alejandra --check` -> 0. elster drvPath NOT evaluated (age-plugin-tpm died, cold cache) — user. Screenshots /tmp/ff71-chrome{,-after}.png.
7.2 Dark Reader colours (Firefox + Edge, both accounts). The "managed policy" seed in the old text does not exist: 4.9.133 never calls `storage.managed` (0 hits in background/index.js; only storage.local/sync), so `3rdparty.Extensions` in policies.json is ignored. Its only input is Settings → Advanced → Import (ui/options exportSettings/importSettings). So: generate a settings JSON from roles (`darkSchemeBackgroundColor` = bg, `darkSchemeTextColor` = fg; defaults #181a1b/#e8e6e3) as a store file / repo artefact, user imports once per browser per account (4 imports); state then lives in each profile (persisted). Check at execution: exact exported-JSON shape (export one from the live addon, change only the two keys).
7.3 Edge chrome. Chromium prefs present in the Entra profile: `browser.theme.color_scheme`, `browser.theme.follows_system_colors` (= "Use GTK"), `browser.theme.user_color` (strings of msedge); managed policies dir already used (layer-compositor.nix:387). `BrowserThemeColor`-style policy names were NOT found as literal strings in msedge (policy templates may be in a .pak) → unverified. Measure first: does Edge with follows_system_colors pick up the GTK Kanagawa headerbar (screenshot). DECISION (below).
7.4 o365 apps (teams-for-linux 2.17.1). Launcher = rust_o365 `o365` script → `teams-for-linux --user-data-dir=~/.config/o365-profiles/<App>` (o365:31,43), one Electron per M365 web app. Every instance reads /etc/teams-for-linux/config.json first (auth-entra.nix:519-537, already ours, only `auth.intune`). Levers in 2.17.1 (app.asar):
  - `followSystemTheme` (boolean, default false; asar :261635): Teams V2 only — sets Teams' React `clientPreferences.theme.userTheme = "dark"` from nativeTheme (asar :259054-259066). Gives Teams' OWN dark theme, not the palette; no effect on Outlook/Word/Excel/… (they are not the Teams client).
  - `customCSSLocation` (asar :310427-310470): injected into the page AND into every iframe (Teams V2 iframe workaround) for ALL o365 profiles, since config.json is global. A palette CSS overriding Fluent UI tokens (`--colorNeutralBackground1` …) could recolour Teams/Outlook web; fragile against Microsoft's weekly frontend changes, and unmeasured (needs a signed-in mailbox to look at, which Phase 0 refused to open).
  Also 2.17 moved flat keys under `appearance.*` (docs table in asar: `followSystemTheme` → `appearance.followSystemTheme`, `customCSSLocation` → `appearance.cssLocation`) — check which one 2.17.1 actually reads before writing (the intune flag broke exactly this way, auth-entra.nix:519).
  DECISION (below).
  7.4 DONE 2026-09 auth-entra.nix: `followSystemTheme = true` (flat) next to `auth.intune` in /etc/teams-for-linux/config.json. Key checked in 2.17.1 app.asar: config schema defines flat `followSystemTheme` (default false, restart); the `appearance.*` table says "None of these nested targets are implemented yet. The flat names … are the only names the app accepts today". Main process sends `nativeTheme.shouldUseDarkColors` 2.5 s after load + on change; preload sets Teams V2 `userTheme = dark|default`. Input: portal `org.freedesktop.appearance color-scheme` = 1 (prefer-dark) on vkokurin (busctl ReadOne), Electron 42.9.3.
  verified 7.4: `alejandra --check auth-entra.nix` -> 0; elster eval NOT run (age-plugin-tpm, cold cache) — user @ 2026-09.
  7.4 user test 2026-09: Teams "как был с Канагавой, так и остался" (Teams' own dark was already chosen in its account settings, so the flag is a no-op here; kept for fresh profiles), Outlook not Kanagawa (expected, 7.4 does not reach it). Palette in Outlook only via option (b) customCSSLocation — not taken.
7.5 Chat/media apps — none has a file Nix can own:
  - Mattermost (both accounts): channel colours are a per-user SERVER setting (Settings → Display → Theme → Custom, paste a JSON of ~20 keys: sidebarBg, centerChannelBg, …); desktop `~/.config/Mattermost/config.json` only has `darkMode: false`, `themeSyncing: true` (app chrome follows the server theme). Artefact: theme JSON from roles, user pastes once per server account.
  - Slack 4.51.180 (Entra, nixpak): per-workspace server-side theme; whether 4.51 still accepts the legacy custom-colour paste string is UNVERIFIED → check in the app; else exception (Slack's own dark).
  - Telegram 7.0.2 (Entra, nixpak): `.tdesktop-theme` = zip with `colors.tdesktop-theme` (key: #rrggbb lines) generated from roles; user opens it once in the app (file must be under ~/Downloads, the only rw bind, packages.nix:89); stored in tdata (`.var/app/org.telegram.desktop`, persisted auth-entra.nix:287). Needs the key list of 7.0.2 (upstream `Telegram/Resources/colors.palette`), several hundred keys, most derivable.
  - SimpleX 7.0.1 (both): Compose app with in-app theme editor + YAML import/export (`ThemeOverrides`, `AppPreferences.readThemeOverrides`/`writeThemeOverrides` in common-desktop jar); preferences dir (upstream: ~/.config/simplex) does not exist on vkokurin and is NOT persisted (only ~/.local/share/simplex, layer-users-local.nix:113, auth-entra.nix:324) → an imported theme would be erased at reboot. Needs a persistence entry per account, or exception.
  - Grayjay 17 (Entra): CEF app, UI CSS baked in `lib/grayjay/wwwroot/web` (33 literal #1b1b1b), no theme setting found. Only path = build-time CSS rewrite (overrideAttrs, packages.nix:328) — fragile, rarely looked at. Recommended: exception.
  DECISION (below).
7.6 Horizon (gm, Entra). Correction to 0.3's "override GTK_THEME after the wrapper": our launcher exports env BEFORE `exec horizon-client` (packages.nix:291-307), the FHS runScript is `horizon-client_wrapper`, which does `export GTK_THEME='Adwaita'` last (nixpkgs package.nix:63-68, makeWrapper `--set`) → any GTK_THEME from our side is overwritten. Mechanism that works: in our existing `buildFHSEnv` override (packages.nix:256-270) replace `runScript` for pname horizon-client with a copy of the wrapper where `GTK_THEME='Adwaita'` → `GTK_THEME='Kanagawa:dark'` (runCommand + substituteInPlace; fails the build if the line moves). Theme lookup: ~/.nix-profile/share/themes/Kanagawa exists on the Entra home and ~/.nix-profile/share is on the session XDG_DATA_DIRS, which the launcher prefixes (not replaces, packages.nix:298); the FHS env binds /home and /nix. `XDG_CONFIG_HOME=~/.omnissa/xdg-gm` has no gtk-3.0 → icon theme/cursor/font from settings.ini are lost; GTK_THEME alone carries the colours (the Kanagawa dir already has kanagawaCss prepended, gtk.nix:43-53). Optionally link `xdg-gm/gtk-3.0/settings.ini` to the HM one. Risk named by nixpkgs (package.nix:61-62): "Horizon is prone to UI usability issues with non-default themes, such as Adwaita-dark" → test the selector, login, disconnect dialog; fallback = exception. Also check the return-link launch (OpenURI portal env) still has XDG_DATA_DIRS with the profile.
  7.6a DONE 2026-09 (code, uncommitted): packages.nix horizon-gm `buildFHSEnv` override replaces `runScript` for pname horizon-client with a runCommand copy of the wrapper, `substituteInPlace --replace-fail "export GTK_THEME='Adwaita'" "export GTK_THEME='Kanagawa'"` (theme name = gtk.nix `theme.name`, the dir has no `:dark` variant needed: adw-gtk3-dark base). usbarbitrator wrapper untouched.
  verified 7.6a: `nix build .#x86_64-linux.workstation.packages.horizon-gm` -> 6gcfn16z…-horizon-gm; closure has x6vib9d5…-horizon-client_wrapper with `export GTK_THEME='Kanagawa'`, usbarbitrator-init still points at the original wrapper; `alejandra --check` -> 0. Headless dwl + Xwayland :1, throwaway HOME: /proc environ GTK_THEME=Kanagawa, XDG_DATA_DIRS has ~/.nix-profile/share. Screenshot: ONLY the menubar goes dark (#222226); selector body stays #f2f2f2, strip #e9e9e9. Cause: the client paints them itself (`gtk_widget_override_background_color`, literals #f2f2f2/#e9e9e9/#929292/#F7F6F6/#ffffff in lib/omnissa/horizon/bin/horizon-client strings; resource horizonClient.css has only paddings + #343434 labels). Probe: `xdg-gm/gtk-3.0/gtk.css` with `* {background-color:#1f1f28; color:#dcd7ba}` (USER priority beats the override) -> body #1f1f28, menubar/strip text palette (tile labels stay black, see 7.6b probe); "Add Server" tile stays white (PNG card, item_background_card.png) @ 2026-09. elster drvPath NOT evaluated (cold TPM cache) — user.
  DECISION owed (7.6b): (a) GTK_THEME + generated `xdg-gm/gtk-3.0/gtk.css` from roles, linked by the launcher (targeted selectors from GTK Inspector, not `*`), tiles stay white PNGs → row partial→themed if user accepts; (b) GTK_THEME only → row partial (menubar/dialogs dark, body light); (c) revert, exception (nixpkgs warns about non-default themes).
  Answered 2026-09: (a).
  7.6b probe 2026-09 (LD_PRELOAD shims in /tmp/hz76, throwaway HOME, not shipped): overrides = strip GtkEventBox ×6 #e9e9e9 (`window > box > notebook > stack > widget`), CdkBackground #f2f2f2 (grid, tile, desktop/spinner pages), GtkSocket/EventBox #000000 (session area, keep). CdkBackground paints itself; CSS reaches only its child box: `viewport > widget > box, notebook > stack > widget > box.vertical` -> body #1f1f28, strip #2a2a37 (measured). BLOCKER: tile labels (CdkLabel "Add Server"/desktop names) are drawn with `cairo_set_source_rgb(0,0,0)` (18 calls logged), CSS `color` on CdkLabel/CdkBrokerItem/`*` has no effect → black on #1f1f28 = unreadable. Behind-label backgrounds (#CdkLabel, #CdkBrokerItem, its box/widget, #EventBox-1, CdkGrid) do not paint. Only a light body keeps labels readable (body #dcd7ba measured: black label readable, tile PNG white).
  DECISION owed (7.6c): (a1) strip + menubar dark, body = a light palette colour (fg #dcd7ba) so black labels stay readable; (a2) strip dark, body stock #f2f2f2; (a3) dark body + binary patch of the label colour (hack, not recommended); fallback (b).
  Answered 2026-09: (a1).
  7.6c DONE 2026-09 packages.nix horizon-gm: `gtkCss` (writeText from roles: strip bgAlt, body fg bg + `color: bg` for theme-drawn text) linked by the launcher to `xdg-gm/gtk-3.0/gtk.css` (next to mimeapps.list; USER priority beats the client's override_background_color).
  verified 7.6c: `alejandra --check` -> 0; the css rendered from theme.nix roles, headless dwl + throwaway HOME: menubar #222226, strip #2a2a37, body #dcd7ba, "Add Server" caption black on #dcd7ba readable, tile card = client's own light plate + white PNG @ 2026-09. `nix build .#…horizon-gm` of the final version NOT run: age-plugin-tpm died (cold TPM cache) after the 7.6a build — user (`unlock-secrets`, then build / rebuild). Throwaway dirs /tmp/hz76 removed.
  7.6 user test 2026-09 (screenshots): selector + desktop list OK, but the Disconnect confirmation dialog body is blank #ffffff (message text invisible: theme fg on the client's white). User: keep Horizon as is → exception. 7.6 REVERTED: packages.nix back to 9a64f2d^ (stock wrapper, no gtk.css); the launcher no longer links `~/.omnissa/xdg-gm/gtk-3.0/gtk.css`, the old symlink must be removed by hand (GTK would keep reading it).
7.7 User test per app after 7.1–7.6 land, checklist written then (each app's main window, a dialog, a menu; browsers: new tab, settings, a site under Dark Reader; Horizon: selector + login + disconnect dialog; o365: Teams chat + Outlook inbox if 7.4 ≠ exception). Imports (Dark Reader ×4, Mattermost, Telegram, SimpleX) are done by the user and are part of the test.
Exit criteria: every row in scope is themed (user accepted 7.7 for it) or exception with a one-line reason; for imported artefacts the generator is in the repo (from roles) and the import step is written in the row.
Decisions owed before 7.1 (ask one at a time at the start of the execution session):
  - Firefox chrome: (a) measure first, if System theme already follows GTK Kanagawa → inherits, done; (b) userChrome.css from roles, linked into every profile by an activation glob; (c) Dark Reader `changeBrowserTheme` (derived colours); (d) exception.
  - Edge chrome: (a) `follows_system_colors` / GTK mode, measure; (b) Edge's own dark + exception; what is Edge used for daily (it is not the o365 host)?
  - o365: (a) `followSystemTheme = true` only (Teams' own dark, Outlook/Office stay light) → row = partial/exception; (b) (a) + palette CSS via `customCSSLocation` (fragile, needs a signed-in look); (c) exception.
  - Chat set: which of Mattermost / Slack / Telegram / SimpleX / Grayjay get a generated import artefact, which become exceptions (recommended: Mattermost + Telegram artefacts, SimpleX only with a persistence entry, Slack check-in-app, Grayjay exception).
  Answers 2026-09 (partial):
  - Dark Reader: user already picks its BUILT-IN "Kanagawa" colour scheme (bundled config/color-schemes.drconf:85 = bg #1f1f28, text #dcd7ba = palette) → 7.2 needs no generated JSON; the job is "selection survives". User: "не персистится". Measured (Entra home): Firefox storage-sync-v2.sqlite (on rpool/safe/persist, stable profile ubw2338r.default) holds `darkColorScheme: "Kanagawa"`, `darkSchemeBackgroundColor #1f1f28` after today's boot → Firefox DOES keep it; Edge `Sync Extension Settings/eimadpbc…` (also persisted) has `darkColorScheme: "Default"` and its LevelDB log has not been written since 2026-09-16 → the Edge selection is what is lost (or was never written). Hypothesis to test: Edge profile sync (signed-in Entra account) overwrites extension storage.sync from the cloud; or the local account (crookedmirror, not readable from here) is the one losing it. Next step = user says which browser/account, then set it in Edge, restart Edge, re-read the LevelDB.
  - Edge: used for heavy-JS work sites only → chrome theming low value; proposed: content = Dark Reader Kanagawa (above), chrome = exception unless the 7.3 GTK measurement is free. Confirm.
  - Slack: user says it picked up the palette. Measured from the user's screenshot: rail/header #191928, sidebar #14151c, main #1a1d21 (Slack's own dark), selected item #cba6f7 (= catppuccin mocha mauve, a leftover of the old theme, not Kanagawa) → NOT palette; row = partial. Fix is manual in Slack (Preferences → Themes → custom colours: window bg, accent/selection), values from roles handed as a list.
  - o365: user "согласен" — option not identified (a/b/c had no recommendation) → ask again.
  - Firefox chrome, Mattermost / Telegram / SimpleX / Grayjay: still open.
  Answers 2026-09 (round 2):
  - crookedmirror: browsers are not used there → Dark Reader/Firefox/Edge scope = Entra home only; the "не персистится" case = Edge on the Entra home (Firefox keeps Kanagawa, measured above). 7.2 = find why Edge's Dark Reader storage stays "Default" (set Kanagawa, restart Edge, re-read `Sync Extension Settings/eimadpbc…`; if it reverts, suspect Edge sync).
  - Edge: OK → content = Dark Reader Kanagawa, chrome = exception (inherits if the GTK mode picks it up for free).
  - o365: (a) `followSystemTheme = true` in /etc/teams-for-linux/config.json (flat key vs `appearance.followSystemTheme`: check which one 2.17.1 reads). Row → partial: Teams dark (its own), Outlook/Office web light, reason in the row.
  - Slack declaratively? The UI is app.slack.com loaded remotely; the theme is a per-user SERVER pref, so Nix owns no file. Options: (a) generate the custom-theme string from roles in the repo, user applies it once (Preferences → Themes → custom), it then follows the account across reboots and machines; (b) build-time app.asar patch in our `slack` overrideAttrs (packages.nix:228) injecting a CSS that overrides Slack's `--sk_*` variables (present in app.asar) on every load — fully declarative, but a patch of a proprietary binary that breaks silently with Slack's frontend changes, and whether Electron's asar integrity fuse is enforced on Linux is unverified. Recommended (a). User to pick.
  Answered 2026-09: Slack = (a) generated custom-theme string from roles, applied once by the user.
  Answered 2026-09 (round 3, all recommended options):
  - Firefox chrome: measure first (headless dwl + throwaway profile, System theme auto vs GTK Kanagawa); if it follows GTK → inherits, else come back with (b) userChrome.css / (c) Dark Reader changeBrowserTheme.
  - Mattermost: generated custom theme JSON from roles, user pastes once per server account (both accounts).
  - Telegram: generated `.tdesktop-theme` from roles (key list from upstream colors.palette of 7.0.2), user opens it once from ~/Downloads.
  - SimpleX: exception (prefs dir unpersisted; built-in dark).
  - Grayjay: exception (no theme knob, CSS baked in; own dark #1b1b1b close to bg).
  Execution order: 7.6 Horizon → 7.1 Firefox measurement → 7.4 o365 followSystemTheme → 7.2 Edge Dark Reader persistence → generators (Slack, Mattermost, Telegram) → 7.7 user test.

## Phase 8 — colors → roles (stretch) ⏳
8.1 Move the 58 `theme.colors` refs (via `k`/`c` aliases: default 6, bar 1, launcher 7, torrent 18, notify 3, screenshot 5, qt 4, lock 14) in home/default.nix and desktop/{bar,launcher,torrent,notify,screenshot,qt,lock}.nix onto roles.
8.2 User test only if drvPath changed: quick look at bar, launcher, lock screen, notifications, qBittorrent.
Exit criteria: `theme.colors` referenced only inside `cells/common/theme.nix`; elster drvPath unchanged across the phase (or user accepted 8.2).

## Fallback at any phase
Phases after 1 are independent; stop anywhere and what is merged stays coherent. Minimum useful slice = 0 + 1 + 2 (fixes delta/lazygit), one session.

## Progress
- 2026-09: plan written, then rewritten around a full app inventory after review ("нет keepass, horizon, claude, hermes"). Palette already in `cells/common/theme.nix` (0251f2f). Lazygit state persistence fixed separately, not part of this plan.
- 2026-09: added the user acceptance gate (invariant + per-phase test step): the user asked to be made to test every app after its theme lands.
- 2026-09: Phase 0 done, read-only (headless dwl + private tmux, nothing on the real session touched). Surprises: KeePassXC and Vial do NOT inherit Qt; Horizon is forced to Adwaita by the nixpkgs wrapper; o365 = teams-for-linux, not Edge; skim/fzf/nmtui use their own colours; all 0.4 capabilities = yes. Phase 0 changes no app, so there is nothing for the user to test.
- 2026-09: Phase 1 done: 4 winter* colours + diff*/success/warning/info roles, additions only. drvPath check pending on the user (TPM).
- 2026-09: plan-audit of phases 2-8 against live code: fixed skim `colors` (no such HM option), KeePassXC via HM settings (would make the ini read-only), zathura not an HM program yet, nvim counts/missed files, Claude theme = custom:kanagawa (was a conditional), added lazygit authorColors (2.2b), renumbered 5.5/5.6.
- 2026-09: Phase 2.1-2.2b code landed (bat tmTheme, delta styles from diff* roles, lazygit authorColors); verified off-host, awaiting rebuild + 2.3.
- 2026-09: user accepted 2.3 ("всё ок"); found no Markdown highlighting in skim preview → 2.4 (tmTheme lacks markup scopes).
- 2026-09: 2.4 landed (markup rules patched into upstream tmTheme, + oniViolet2); awaiting rebuild + 2.4 user test.
- 2026-09: user accepted 2.4 ("всё ок"); Phase 2 ✅. Phase 3 in a fresh session.
- 2026-09: 3.1 landed (btop theme generated from roles/colours); verified off-host, awaiting rebuild + 3.2.
- 2026-09: user accepted 3.2 ("ок"); Phase 3 ✅. Phase 4 in a fresh session.
- 2026-09: plan-audit of Phase 4: 7 files with catppuccin (plan had 4), 7 on_load("catppuccin") wrappers that would silently kill bufferline/modes/smear/blame/sniprun, darken used 31× and kanagawa lib.color is not equivalent, no_italic lost, terminal slot 0 differs, 12 integrations with no kanagawa groups. 4.2 switched to a catppuccin-shaped palette map (191 refs untouched). 3 decisions owed.
- 2026-09: 3 decisions answered (slot 0 override, reactive deleted, blame 14 via map); 4.1+4.2 landed in one commit, verified headless off the real data dir; awaiting `:Lazy sync` + `:Lazy clean` on both accounts + 4.3.
- 2026-09: user accepted 4.3 ("ок!"); Phase 4 ✅. Phase 5 in a fresh session.
- 2026-09: plan-audit of Phase 5: Vial env-var plan cannot work (app sets its own palette; settings unpersisted) → decision; Ctrl-R (histdb-skim) ignores SKIM_DEFAULT_OPTIONS, needs HISTDB_COLOR; skim is 5.4 not 0.10; mpv OSC needs osc script-opts, not osd-*; KeePassXC recent-db list lives in ~/.cache ini (plan said ~/.config); p10k has 1 cube colour, not 2; fzf has no in-repo consumer.
- 2026-09: Vial decision: exception.
- 2026-09: 5.1 landed (zathura via programs.zathura from roles); verified off-host. User test of Zathura waits for the batched 5.6 after 5.2-5.5 land (one rebuild for the phase).
- 2026-09: 5.2-5.5 landed (mpv osd+osc, avizo + `-d` icons in dwl/waybar, newt/skim/histdb/fzf/p10k, KeePassXC classic); verified off-host. Matrix rows → awaiting user test; 5.6 checklist handed over.

- 2026-09: user accepted 5.6 (all apps "ок"; fzf/Ctrl-R after a fresh tmux pane); Phase 5 ✅. Phase 6 in a fresh session.

- 2026-09: plan-audit of Phase 6 against the live binaries: hermes skin goes through seedHermes, not HM; hermes diff bgs + banner art are hardcoded (decision on banner); Claude silently drops override keys not in the base palette (70 keys listed); opencode built-in kanagawa is off-palette (yellows, 7 diff hexes) → own theme file (decision); exit criterion "bg #1f1f28" was vacuous → SGR histogram.

- 2026-09: Phase 6 decisions: hermes banner = `display.compact true`; opencode = own kanagawa.json from roles.

- 2026-09: 6.1 landed: hermes skin from roles + build-time recoloured banner art (compact turned out dead in 0.20.5, user switched to (a)); pt chrome is 256-colour in hermes (bypasses PROMPT_TOOLKIT_COLOR_DEPTH) → accepted as off-palette by default, user may choose the patch at 6.4.

- 2026-09: 6.2 landed: Claude Code custom theme (72 keys, all accepted by key set) + `custom:kanagawa` in settings.json.

- 2026-09: 6.3 landed: opencode themes/kanagawa.json from roles (50 keys, shadows the built-in) + tui.json theme kanagawa. Phase 6 code complete; rows → awaiting rebuild + 6.4.

- 2026-09: user accepted 6.4 (hermes, claude, opencode "ок"); Phase 6 ✅. Phase 7 in a fresh session, starting with plan-audit (Phase 7 is a sketch).

- 2026-09: user found the hermes status-bar session-title badge gold. Cause: `status-bar-session-title` ('bg:#FFD700 #1a1a2e bold', cli.py:19971) lives only in `_tui_style_base`; `get_prompt_toolkit_style_overrides` (skin_engine.py:988) never returns it, so no skin key reaches it. Same for `status-bar-yolo` #FF4444, `clarify-answer` #98FB98, `voice-prompt/-recording/-processing` (diffed base dict vs override dict). 6.1 audit grepped `get_color(` only and missed base-only classes. Hermes row → wrong, Phase 6 back to ⏳.

- 2026-09: 6.5 decision (a) patch: cells/repo/hermes-skin-pt-classes.patch adds skin keys status_bar_title_bg/_text, status_bar_yolo, clarify_answer, voice_prompt/_recording/_processing to get_prompt_toolkit_style_overrides (defaults = old base values, so other skins unchanged); applied via overrideAttrs in cells/repo/packages.nix; skin sets them from roles (badge focus/bg, yolo urgent, answer success, voice info/urgent/warning). Upstream PR still to open.
  verified 6.5: `nix build .#x86_64-linux.repo.packages.hermes-agent` -> hsawfgwy…-nixq (patch applies); skin yaml built via agent-proxy seed has the 7 keys; get_prompt_toolkit_style_overrides() under a throwaway HERMES_HOME with that skin: base-only classes = [], badge `bg:#7e9cd8 #1f1f28 bold`, 12 distinct hexes, 0 off-palette; remaining literals in cli.py = exception fallbacks + the out-of-credits panel (#CD7F32, rare) @ 2026-09. elster drvPath NOT evaluated (cold TPM cache) — user.

- 2026-09: first 6.5 look still gold: the session ran the unpatched hermes from a stale the-hive devshell (nix-rensa-profile precedes /run/current-system/sw in PATH); after `direnv reload` user: "работает". Phase 6 ✅. Open follow-up (not blocking): upstream PR for the 6 base-only pt classes, then drop cells/repo/hermes-skin-pt-classes.patch.

- 2026-09: plan-audit of Phase 7: Dark Reader has no managed-storage seed (import only); own Firefox static theme blocked by signing; Horizon GTK_THEME cannot be set from the launcher (wrapper `--set` runs last) → patch runScript; o365 = one global teams-for-linux config, `followSystemTheme` is Teams-only and not palette, `customCSSLocation` reaches all apps; Mattermost/Slack themes are server-side, Telegram/SimpleX in-app imports, SimpleX prefs unpersisted; Grayjay has no theme knob. 4 decisions owed.
- 2026-09: Phase 7 decisions answered: browsers Entra-only; Edge chrome exception; o365 followSystemTheme; Slack/Mattermost/Telegram = generated import artefacts; Firefox chrome = measure first; SimpleX + Grayjay exception.

- 2026-09: 7.6a landed (Horizon wrapper GTK_THEME=Kanagawa), uncommitted; measured: only the menubar follows, the client hard-paints its body #f2f2f2 → decision 7.6b.
- 2026-09: 7.6b/c: user chose gtk.css (a), then light body (a1) since tile captions are hard-black; 7.6 committed, test in 7.7.
- 2026-09: user rebuilt; live `horizon-gm` = whsb1cw8…, links zmnx9y3b…-horizon-gm-gtk.css (bgAlt/fg/bg), FHS wrapper `export GTK_THEME='Kanagawa'`. Horizon row → awaiting user test (checklist handed over).
- 2026-09: user test: disconnect dialog text blank; user "оставим как есть" → Horizon = exception, 7.6 reverted (needs rebuild + rm of the xdg-gm gtk.css symlink).

- 2026-09: 7.1a measured: Firefox System theme takes only the tabstrip from GTK (#2a2a37); toolbar/urlbar are its own blends (#414148/#2d2d32) → decision 7.1b.
- 2026-09: Horizon revert rebuilt by the user; xdg-gm/gtk-3.0 is empty. 7.1b (b) landed: userChrome.css from roles via activation; verified headless. Firefox chrome → awaiting user test (batched in 7.7).
- 2026-09: out of scope, found while here: /etc/firefox/phoenix.cfg pointed at `programs.firefox.package` (no autoConfig appended) → none of our prefs loaded, Phoenix sanitize-on-shutdown wiped tabs/history; fixed to `finalPackage` (e908f3a). User reported primary buttons in chrome popups (translation) unreadable: white text on the new #7e9cd8 accent → button-*-primary tokens from roles (eafc6fb).
- 2026-09: user rebuilt and accepted Firefox chrome + session restore ("всем доволен"). 7.1 ✅.

- 2026-09: 7.4 landed: teams-for-linux `followSystemTheme = true` (flat key; 2.17.1 ignores `appearance.*`). o365 row → awaiting rebuild + test (batched in 7.7).
- 2026-09: user: Teams unchanged (already dark), Outlook light, Edge has NO Dark Reader at all. Cause: Edge forcelist used the Chrome Web Store id eimadpbc…; Edge Add-ons updatecheck for it = `error-unknownApplication`, for ifoakfbpdcdoeenechcleahebpibofpc (Edge store Dark Reader) = ok; Preferences has only a `lastpingday` stub for eimadpbc…, no Extensions/ dir. The earlier "Edge storage says Default" read was that stub's leftover storage, not a live addon. Fixed id in layer-compositor.nix. 7.2 = after rebuild: Dark Reader appears in Edge, pick Kanagawa, restart, check it sticks.
- 2026-09: user: Edge Dark Reader "другой, с Premium themes". Checked: genuine (manifest author Alexander Shutau, 4.9.133 = Firefox's, update_url Edge store); the Chromium build's popup has a "Dark Reader Plus" preset grid (free Default/Gray/Dimmed, rest paywalled, ui/popup ThemePicker), absent in the Firefox xpi. Kanagawa is still in config/color-schemes.drconf:85, picked via Theme → Colors → Color scheme dropdown (ColorsGroup), not the preset tiles.
- 2026-09: user screenshot (Confluence in Edge): bg #181a1b = Dark Reader Default, not Kanagawa. Edge `Sync Extension Settings/ifoakfbp…/000003.log` (written 13:22): every record `darkColorScheme: Default`, `darkSchemeBackgroundColor #181a1b` → Kanagawa never got written, it is not a persistence loss. The Chromium popup hides the scheme dropdown; storage.managed 0 hits in this build too. Proposed: Firefox Dark Reader (already Kanagawa) → Settings → Advanced → Export → Edge → Import.
- 2026-09: user imported; now two Dark Readers. Measured: forced ifoakfbp… (location 7 = policy) holds `darkColorScheme: Kanagawa`, #1f1f28 → import worked. Second = eimadpbc… from the Chrome Web Store (update_url clients2.google.com, from_webstore, first_install 13:25:00), Edge sync on with `extensions: true` → the account's old CWS copy, reinstalled by sync once the forcelist stopped claiming that id. Its storage = Default. Fix: `ExtensionInstallBlocklist = [eimadpbc…]` in the same Edge policy file.
- 2026-09: user asked "мы это декларируем?" → policy = declared, the scheme = addon storage (no managed input) → user chose a generated import file. home/desktop/darkreader.nix: `~/.config/darkreader/kanagawa.json` = `{theme}` only (import merges top level: UserStorage.set `{...settings, ...$settings}`, validateSettings checks present keys only), 20 theme keys = the record the Firefox import wrote in Edge, bg/fg from roles. Import step: Dark Reader → Settings → Advanced → Import Settings → that file; redo after a palette change.
  verified: standalone build -> w9pgkxml…-darkreader-kanagawa.json, keys/values == Edge LevelDB Kanagawa record; `alejandra --check` -> 0 @ 2026-09.
- 2026-09: user rebuilt: one Dark Reader in Edge (policy), sites in Kanagawa after restart — "всё так". 7.2 ✅. Rest of Phase 7 in a fresh session.

Next: 7.5 generators — Slack custom-theme string, Mattermost theme JSON, Telegram `.tdesktop-theme` (key list from upstream colors.palette of 7.0.2), all from roles; then 7.7 user test.
Blocked on: nothing.
