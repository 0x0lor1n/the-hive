# hisilome-extract — move cells/hisilome into its own flake repo, history preserved, osgiliath unchanged

Status: PLANNING (2026-09). Owner: user; Claude = executor.
Prereq: the-hive at 36c02c3 or later, clean tree (`.scratch/` untracked is fine); hisilome-feedback phase 3 deployed. Do NOT `git rm cells/hisilome` until Phase 2; do NOT edit any file under content/, templates/, static/ in either repo until Phase 3 is ✅ (the diff invariant depends on it).

Target: `github:0x0lor1n/hisilome` is a plain flake (no rensa/std) exposing `packages`, `nixosModules.default`, `devShells.default`; the-hive consumes it as input `hisilome`; osgiliath serves byte-identical `public/` before and after; `cells/hisilome` is gone from the-hive. Stretch: the-hive README no longer mentions zola/liquidsoap at all.
Source refs: `cells/hisilome/packages.nix` (382 lines; `src` filter at :14, all outputs at :285–382 — the only file that knows the tree layout), `cells/hisilome/nixosModules/default.nix` (:53 `cell.packages.site` default — the one option whose default changes shape), `cells/hisilome/nixosModules/nginx.nix` (:35 `cell.packages.nginxHttpConfig`, :52 `cell.packages.nginxLocations` — non-derivation values the module needs from packages.nix), `cells/hisilome/nixosModules/station.nix` (:35, :123, :125, :143 — four `cell.packages.*` refs), `cells/hisilome/nixosModules/setup.nix` (:15–25 `builtins.path ../radio/*` — relative path, survives the move as-is), `cells/hisilome/devshells.nix` (:13 reads `inputs.self.outPath + "/nix/dev.sh"` — the-hive-only, drop), `flake.nix:73` (devshell merge), `cells/repo/devshells.nix:30` (treefmt exclude), `cells/server/profiles/site-hisilome.nix:11` (the consumer import), `README.md:16,59,60,67,68`, `.gitleaks.toml:15`, `cells/hisilome/process-compose.yaml:9` (message hardcodes `cells/hisilome/music/`).
Repo: source cell `cells/hisilome` (57 tracked files, 50 commits since c58026a, `music/` 1.1G gitignored, `public/` and `radio/state/` gitignored). Target `/srv/workspace/projects/hisilome` (decided in 0.2; `/srv` itself is rolled back every boot), devshell `nix develop` → zola, d2, ffmpeg, flac, icecast, liquidsoap, process-compose, watchexec + the ten scripts. the-hive nixpkgs (root input `nixpkgs_2` in flake.lock, the one `transformInputs` instantiates as `inputs.pkgs`) pinned at 34ab99075ac4f7e40cf037eef32cb1c360bb85e9 — NOT d407951…, which is a nested lock node (see Phase 1 progress).
Naming: remote `github:0x0lor1n/hisilome` (name/visibility — see Blocked on); local checkout `/srv/workspace/projects/hisilome` (`$HISILOME` below); split branch in the-hive `hisilome-split` (local only, never pushed, deleted in Phase 4); the-hive flake input `hisilome` with `inputs.nixpkgs.follows = "nixpkgs"`. New repo layout: content/ templates/ static/ syntaxes/ radio/ bin/ config.toml process-compose.yaml process-compose.dev.yaml stay at root (`radio.liq`, `build-site`, `process-compose` all use cwd-relative paths — root must stay the working dir); nix code moves to `nix/packages.nix` (signature `pkgs: { ... }`, same attr names as today plus `nginxHttpConfig`, `nginxLocations`, `devNginxConf`), `nix/modules/{default,setup,station,nginx}.nix`, `nix/devshell.nix`; `flake.nix` at root with inputs `nixpkgs` only. Outputs: `packages.x86_64-linux.{site,build-site,dev-site,dev-nginx,build-queue,tag-replaygain,tag-album,listener-count,station-online,station-offline}`, `packages.x86_64-linux.default = site`, `nixosModules.default`, `devShells.x86_64-linux.default`. Module option namespace stays `services.hisilome.*`; inside the module `hisilome = import ../packages.nix pkgs` (the CONSUMER's pkgs, so the host never mixes two nixpkgs — the flake's own `nixpkgs` input serves only standalone `nix build`/`nix develop`). Store names stay `hisilome-src` / `hisilome-site`. Dev-tree markers: `.envrc` = `use flake`; `.gitleaks.toml` copied from the-hive with only the `0x0lor1n@` allowlist line. Secrets (`secrets/hisilome/*.age`) stay in the-hive — host-facing, not part of the site.

Invariants:
(`$HISILOME` = `/srv/workspace/projects/hisilome`, the local checkout — see 0.2 for why not `/srv/hisilome`.)
- `nix build /srv/the-hive#colmenaHive.toplevel.osgiliath --print-out-paths` succeeds before any `colmena apply`.
- `diff -r "$(nix-store -qR <toplevel> | grep -- '-hisilome-site$')" "$(nix build $HISILOME#site --print-out-paths)"` == empty, from Phase 1.4 until Phase 3 ✅ (no content edits in that window).
- `gitleaks git $HISILOME` == `no leaks found` — kill-switch: a real secret in split history ⇒ do not push; fallback = snapshot import (see Fallback).
- `du -sh $HISILOME/music | cut -f1` == `1.1G` after Phase 1.6; music is moved with `mv`, never copied/deleted.
- `curl -s -o /dev/null -w '%{http_code}' https://hisilo.me/` == `200` and `curl -sI https://hisilo.me/ | grep -i content-security-policy` contains `default-src 'none'` after every deploy.
- `git -C /srv/the-hive log --oneline -- cells/hisilome | wc -l` ≥ 50 — the-hive keeps its own history untouched; nothing is rewritten there.
- Never push `hisilome-split` to origin; never `git push --force` anywhere.

## Phase 0 — Split + leak scan (kill-switch) ✅
0.1 In `/srv/the-hive`: `git subtree split -P cells/hisilome -b hisilome-split`. Record `git rev-list --count hisilome-split` and `git log --format=%h hisilome-split | tail -1` in Progress. — DONE 2026-09, `git subtree split -P cells/hisilome -b hisilome-split` → 50 commits, root `6cdcb05`, tip `9c3c286`, tree = the 14 top-level entries of the cell (no `music/`, no `public/`).
0.2 `git clone -b hisilome-split /srv/the-hive /srv/workspace/projects/hisilome && git -C … branch -m main && git -C … remote remove origin`. — DONE 2026-09.
    Target path decided: **B**, `/srv/workspace/projects/hisilome`. `/srv/hisilome` was dropped: `/srv` lives on `rpool/local/root`, which `storage-zfs-rollback.nix` rolls back to `@blank` every boot; only the declared `rpool/safe/srv/*` datasets survive, so a plain `sudo mkdir /srv/hisilome` would have taken 1.1G of `music/` with it at the next reboot (1.6 `mv`s the only copy). `/srv/workspace` is already a persistent dataset, `2770 <local>:hive` with an inherited hive ACL and a direnv whitelist prefix (`srv-workspace.nix`) — no sudo, no fleet change. Option A (new `rpool/safe/srv/hisilome` dataset + a `srv-hisilome.nix` profile) was rejected as fleet weight for a tree that becomes an external input in Phase 2.
    Two flags the clone needed, neither in the plan: `-c safe.directory=/srv/the-hive/.git` (the-hive is owned by the local user, the agent runs as the Entra user in group `hive`; transient `-c`, the user's git config was not touched) and `--no-hardlinks` (a same-filesystem clone hardlinks objects by default — the new repo must not share inodes with the-hive's object store).
    Verified: 50 commits, root `6cdcb05`, tip `9c3c286`, on `main`, no remote, no `.git/objects/info/alternates`. 57 tracked files, list byte-identical to `git ls-files cells/hisilome`. `diff -r` vs the cell shows only `music/`, `public/`, `radio/state/` and six `content/**/[0-9]*.svg` — all gitignored; the SVGs are d2 build output and every `.d2` source is present. Checkout is `2770 <entra>:hive`.
    Note for Phase 1: commits will be authored by the Entra account unless `-c user.name/user.email` is passed, as in the-hive (`0x0lor1n <0xolorin@users.noreply.github.com>`, no git identity is configured on this host).
0.3 `gitleaks git $HISILOME` (whole history). Anything beyond the known public contact ⇒ kill-switch. — DONE 2026-09, run in-place against the same 50 commits (`gitleaks git . --log-opts=hisilome-split`, 8.30.1) before the clone existed; identical objects, so the result carries over to `$HISILOME`.
    Result: **no leaks**. Default ruleset alone, repo ruleset, and repo PII rules with the allowlist stripped were all run; scanner proven live against a planted `ghp_`/`sk_live_` canary (2 findings), so the clean result is not a silent false pass.
    Correction to the plan: the expected `radio/icecast.xml:16–19` `hackme` ×3 finding does NOT occur — gitleaks' default rules do not flag it. No icecast allowlist entry is needed in the new `.gitleaks.toml`.
    Only PII hit, with the repo allowlist removed: `0x0lor1n@projectsegfau.lt` ×4 in `static/listen.html`, `templates/base.html`, `templates/macros.html` — the intentional public contact. Confirms 1.7: the new `.gitleaks.toml` needs exactly the `0x0lor1n@` allowlist line and nothing else.
0.4 Baseline: `nix build /srv/the-hive#colmenaHive.toplevel.osgiliath --print-out-paths` → write the toplevel path and `nix-store -qR <toplevel> | grep -- '-hisilome-site$'` into Progress as `BASELINE_TOPLEVEL` / `BASELINE_SITE`. Every later diff compares against `BASELINE_SITE`. — DONE 2026-09, run by the user as the local account (see below for why it cannot be the agent).
    Agent-side verification of the two pasted paths: both exist and pass `nix-store --verify-path`; `nix-store -qR BASELINE_TOPLEVEL | grep -- '-hisilome-site$'` returns BASELINE_SITE and nothing else; the tree is a zola output (`index.html`, `style.css`, `rss.xml`, `listen/`, `zero-js-radio/`, …) and `grep -c '<script' index.html` == 0, matching the 3.2 zero-JS check.
    Why user-only: eval calls `extraBuiltins.rageImportEncrypted` on `secrets/globals.nix.age`, and the plaintext cache `/var/tmp/nix-import-encrypted` is `0700 <local>:users` **by design** (`layer-users-local.nix:122–136` — the local user is the only trusted-user / PIN holder / rebuilder). The agent runs as the Entra account: it can neither read that cache nor create its own `$UID` subdir, so `unlock-secrets` does not help it. Any step that evaluates a host toplevel is the user's: this one and 2.5's `NEW_TOPLEVEL`.
    1.8 is NOT affected — BASELINE_SITE is now a literal store path, so the agent can run that `diff -r` unaided.
Exit criteria: `hisilome-split` exists with ≥ 40 commits ✅ (50); `$HISILOME` is a git repo on `main` with `ls` == the 57 tracked files' tree (no `music/`, no `public/`) ✅; gitleaks clean ✅; `BASELINE_SITE` recorded in Progress ✅. Nothing committed to the-hive except this file ✅.

## Phase 1 — Standalone flake in $HISILOME ✅
1.1 `git mv packages.nix nix/packages.nix; git mv nixosModules nix/modules; git mv devshells.nix nix/devshell.nix; git rm flake.nix` (the 178 B cell flake). Commit `layout: nix code under nix/`.
1.2 `nix/packages.nix`: header `{ inputs, cell, ... }: let pkgs = inputs.pkgs;` → `pkgs: let`; `src = builtins.path { path = ./.; …}` → `path = ../.;` and `rel = lib.removePrefix (toString ../. + "/")`; `cell.packages.dev-nginx` (:318) → `dev-nginx` (already in scope after the `in {` block — hoist `dev-nginx` above `dev-site` or use `rec`). Add `inherit nginxHttpConfig nginxLocations devNginxConf;` to the returned set.
1.3 `nix/modules/*.nix`: drop the outer `{ inputs, cell, ... }:` wrapper and `args`; each file becomes a plain NixOS module `{ config, lib, pkgs, ... }: let hisilome = import ../packages.nix pkgs; in …`; replace `cell.packages.X` → `hisilome.X` at default.nix:53–54 (defaultText `"hisilome.site"`), nginx.nix:35,52, station.nix:35,123,125,143. `default.nix` `imports = [ ./setup.nix ./station.nix ./nginx.nix ]`.
1.4 `flake.nix`: inputs `nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable"`; outputs `packages.x86_64-linux = (import ./nix/packages.nix pkgs) // { default = site }` (filter to the ten derivations — `nginxHttpConfig` is a string and would break `nix flake check`), `nixosModules.default = import ./nix/modules/default.nix`, `devShells.x86_64-linux.default = import ./nix/devshell.nix pkgs hisilomePkgs`. Pin lock: `nix flake lock --override-input nixpkgs github:NixOS/nixpkgs/d407951447dcd00442e97087bf374aad70c04cea` so the diff in 1.8 is against the same nixpkgs.
1.5 `nix/devshell.nix`: signature `pkgs: hisilome: pkgs.mkShellNoCC {…}`; drop the `dev` wrapper (the-hive-only) and its shellHook line; shellHook NOTE line → `run these from the repo root`. `process-compose.yaml:9` message → `music/ (gitignored; rsync it in)`.
1.6 `mv /srv/the-hive/cells/hisilome/music $HISILOME/music; mv /srv/the-hive/cells/hisilome/radio/state $HISILOME/radio/state` (both gitignored, nothing to commit). `.gitignore` from the cell already covers them.
1.7 `.envrc` = `use flake`; `.gitleaks.toml` (copy of the-hive's with the icecast allowlist from 0.3); `README.md` — what it is, `nix build`, `nix develop` + `dev-site`, `process-compose up`, how to consume the module (`services.hisilome.{enable,domain,stateDir,sourcePasswordFile,adminPasswordFile}`). `direnv allow`.
1.8 `nix flake check $HISILOME` and `diff -r BASELINE_SITE "$(nix build $HISILOME#site --print-out-paths)"`; `nix develop $HISILOME -c build-site` writes `public/` (not committed); `nix eval $HISILOME#nixosModules.default --apply builtins.isFunction` == `true`.
1.9 After the user confirms the name: create the GitHub repo (user, web UI or `gh repo create`), `git remote add origin git@github.com:0x0lor1n/hisilome.git`, `git push -u origin main`.
Exit criteria: `nix flake check` clean; `diff -r` against BASELINE_SITE empty; `du -sh $HISILOME/music` == 1.1G and `cells/hisilome/music` absent; `git -C $HISILOME log --oneline | wc -l` ≥ Phase 0 count + 4; `origin/main` pushed.

## Phase 2 — the-hive consumes the input ✅
2.1 `flake.nix` inputs: `hisilome.url = "github:0x0lor1n/hisilome"; hisilome.inputs.nixpkgs.follows = "nixpkgs";`. `nix flake lock` (or `--override-input hisilome path:$HISILOME` while iterating before 1.9 lands).
2.2 `cells/server/profiles/site-hisilome.nix:11` → `imports = [inputs.hisilome.nixosModules.default];`; comment line 2 → points at the new repo.
2.3 `flake.nix:73` delete the `// ren.get self [["hisilome" "devshells"]]` line; `cells/repo/devshells.nix:30` delete `"cells/hisilome/**"`; `git rm -r cells/hisilome`.
2.4 `README.md:16` drop the cell line; `:59–60,67–68` replace with one line: site + station live in `github:0x0lor1n/hisilome`. `.gitleaks.toml:15` delete the `0x0lor1n@` allowlist (no longer in tree — verify with `gitleaks dir .` first; keep if it trips on secrets/ or docs/).
2.5 `nix build .#colmenaHive.toplevel.osgiliath --print-out-paths` → `NEW_TOPLEVEL`; `diff -r BASELINE_SITE "$(nix-store -qR NEW_TOPLEVEL | grep -- '-hisilome-site$')"` empty; `nix-store -q --references NEW_TOPLEVEL | grep -c hisilome-src` == 0 is NOT required (site is built by the module from the input's tree; just check the diff). `nix run .#treefmt -- --ci` or the repo pre-commit hook clean.
2.6 One commit: `hisilome: consume github:0x0lor1n/hisilome, drop cells/hisilome`. `git diff --stat HEAD~1 | tail -1` shows ~57 deletions + 5 edits.
Exit criteria: the-hive builds osgiliath toplevel with `cells/hisilome` absent; site diff vs BASELINE_SITE empty; `grep -rn hisilome --include=*.nix . | grep -v secrets` lists only `site-hisilome.nix`, `deploy-osgiliath.nix`, `nixosConfigurations.nix`, `flake.nix`, `layer-session.nix:105` (comment), `palettes.nix:45` (comment).

## Phase 3 — Deploy ⏳ (next)
3.1 `colmena apply --on osgiliath` from the-hive devshell (`dev`).
3.2 Live checks: the two `curl` invariants; `curl -s https://hisilo.me/ | grep -c '<script'` == 0; `curl -s -o /dev/null -w '%{http_code}' https://hisilo.me/listen/` == 200; `ssh osgiliath systemctl is-active hisilome-setup icecast liquidsoap` all `active`; a listener-count fragment under stateDir updated within 60 s (`ssh osgiliath find <stateDir>/radio/state -newermt '-60 seconds' | head -1` non-empty).
3.3 `git -C /srv/the-hive commit` state file `state: hisilome-extract — phase 3 deployed`.
Exit criteria: all 3.2 checks green; `journalctl -u liquidsoap --since -5min` on osgiliath shows no restart loop.

## Phase 4 — Cleanup ⏳
4.1 `git -C /srv/the-hive branch -D hisilome-split`. `$HISILOME`: `nix flake update` is NOT done here (nixpkgs follows the-hive on the host; standalone lock can drift later, separately).
4.2 `docs/install-workstation.md:16,96` still refer to `secrets/hisilome/*` — correct, leave. `.hermes/state/hisilome-feedback.md` paths point at `cells/hisilome/...` — historical, leave.
4.3 New repo: add `LICENSE` (user's choice; content vs code may differ — Blocked on), badge-free README done in 1.7. Optionally `nix flake check` in a GitHub Actions workflow — out of scope unless asked.
Exit criteria: `git -C /srv/the-hive branch --list hisilome-split` empty; the-hive `git status` clean; this file `Status: DONE`.

## Fallback at any phase
If 0.3 finds a real secret in history: `git -C $HISILOME checkout --orphan main-clean && git commit -m "import from the-hive@36c02c3 (history squashed)"`; continue from 1.1 with no history (~15 min lost). If Phase 2/3 fails: `git -C /srv/the-hive revert` the 2.6 commit — cells/hisilome comes back byte-identical, `$HISILOME` stays as an independent mirror (mv music back: `mv $HISILOME/music /srv/the-hive/cells/hisilome/music`). Nothing on osgiliath changes until 3.1.

## Progress
- 2026-09: plan written at the-hive 36c02c3. Read: flake.nix, cells/hisilome/{flake,devshells,packages}.nix, nixosModules/{default,setup}.nix, site-hisilome.nix, nix/dev.sh, .envrc, .gitignore, grep of every `hisilome` reference outside the cell (listed in Source refs). Not yet read line-by-line: nixosModules/station.nix and nginx.nix bodies (only their `cell.packages` refs), packages.nix :48–284. `gitleaks` 8.30.1 available in the repo devshell. Waiting: repo name/visibility confirmation.
- 2026-09: Phase 0 ✅. 0.1 `hisilome-split` = 50 commits, root `6cdcb05`, tip `9c3c286` (local branch, never pushed). 0.3 gitleaks clean over all 50 commits, canary-verified; icecast `hackme` is a non-finding (plan corrected). 0.2 cloned to `/srv/workspace/projects/hisilome` (option B — `/srv` is rolled back every boot, so the planned `/srv/hisilome` would have eaten 1.1G of music at the next reboot); all `$HISILOME` in this file = that path. 0.4 run by the user (the eval-time secrets cache is 0700 to the local account by design), paths verified agent-side. `.scratch/` holds throwaway gitleaks configs/reports; untracked, delete whenever.
- BASELINE_TOPLEVEL: `/nix/store/94hjbqiaaaj2sdka677svak0gbzk6g6r-nixos-system-osgiliath-26.11pre-git`
- BASELINE_SITE: `/nix/store/w2svw823lkfrd3zrbn8b9rcqdszhrl37-hisilome-site`

- 2026-09: Phase 1 ✅ except 1.9. `$HISILOME` = 53 commits on `main` (50 + `abdddca` layout, `f703cc3` flake, `1.7` repo files), all authored `0x0lor1n <0xolorin@users.noreply.github.com>`, no remote yet.
    Verified: `nix flake check` clean; `diff -r BASELINE_SITE $(nix build $HISILOME#site)` **empty** (`/nix/store/45g4919qm2va520yps10fmw8d4fbdq5q-hisilome-site`); `nix eval .#nixosModules.default --apply builtins.isFunction` = true; a throwaway `nixosSystem` importing the module evaluates nginx root/11 locations/appendHttpConfig; `SITE_DRAFTS=0 nix develop -c build-site` → `public/` byte-identical to the store output (default drafts-on differs in 9 files, as expected); `gitleaks git .` over all 52 commits: no leaks; `du -sh music` = 1.1G, `cells/hisilome/{music,radio/state}` gone from the-hive; `direnv allow` done.
    Two plan corrections:
    (a) **Pin.** `d407951…` is NOT what the cell was built with — it is the nested `nixpkgs` node in the-hive's flake.lock; the root input is `nixpkgs_2` = `34ab99075ac4f7e40cf037eef32cb1c360bb85e9` (d2 0.8.1 vs 0.7.1 — the first diff against BASELINE_SITE showed every `.svg` and the pages inlining them changed). Locked to `34ab990`; diff empty. the-hive's `transformInputs` overlay only touches `nix`/`nix-plugins` + `allowUnfree`, so plain `legacyPackages` reproduces the cell's build. For Phase 2 the `follows = "nixpkgs"` still holds — the module uses the host's pkgs anyway.
    (b) **setup.nix paths.** `builtins.path ../radio/*` did NOT survive as-is: the file moved one level deeper (`nixosModules/` → `nix/modules/`), so it is now `../../radio/*`. Same for `packages.nix` `src` (`../.`), as planned.
    Also: `devNginxConf` is a `writeText` derivation, so `lib.isDerivation` alone would have exported it as a package — `flake.nix` does `removeAttrs hisilome ["devNginxConf"]` first. `packages.x86_64-linux` = exactly the ten scripts + `site` + `default`. Stale `cells/hisilome` strings fixed in `nix/packages.nix:247` (comment), `process-compose.yaml:9`, `radio/icecast.xml:14`; `content/zero-js-radio/index.md:261` still links to `the-hive/tree/main/cells/hisilome` — content is frozen until Phase 3 ✅, fix in Phase 4.
    the-hive tree: nothing committed yet (this file only; `cells/workstation/packages.nix` was already modified before this work and is not mine).

- 2026-09-21: 1.9 ✅ by user — `github:0x0lor1n/hisilome` public, `origin/main` = `6699255` (`repo: .envrc, .gitleaks.toml, README`).
- 2026-09-21: Phase 2 ✅ — the-hive `8f97b06` `hisilome: consume github:0x0lor1n/hisilome, drop cells/hisilome` (64 files, +38/−4899). flake.lock pins `hisilome` → `6699255`, `hisilome/nixpkgs` follows.
    NEW_TOPLEVEL: `/nix/store/wczymfwjw870mxdh4dxclk3kqgvymz1j-nixos-system-osgiliath-26.11pre-git` (user built; agent verified path + closure). Its hisilome-site = `45g4919q…` — **byte-identical to BASELINE_SITE** (`diff -r` empty). `hisilome-src` refs in closure: 0. Closure delta vs baseline `94hjbq…` = hash churn only (nginx.conf, setup script identical modulo store hashes) + 1 comment line in icecast.xml (`nixosModules/setup.nix` → `nix/modules/setup.nix`). `treefmt --ci` and pre-commit (treefmt, gitleaks) clean.
    Deviations from 2.4: `.gitleaks.toml` **keeps** `0x0lor1n@` (plan said "keep if it trips on docs" — it trips on this file, line 31); README `dev hisilome` example replaced with `dev workstation` rather than deleted; `nix/dev.sh:6` comment same. Also removed the gitignored leftover `cells/hisilome/public/` (zola output).
    Env gotcha: `/var/tmp/nix-import-encrypted/` was 700 crookedmirror, so eval under the Entra uid failed at `globals.nix` (`mkdir … Permission denied`); user `chmod 1777` it. `nix/rageImportEncrypted.sh:22` could create the root with 1777 itself — small follow-up, not part of this extract.

Next: Phase 3 — deploy `8f97b06` to osgiliath (colmena apply from the devshell; needs TPM PIN → user runs it).
Blocked on: LICENSE choice for the new repo — needed by 4.3 only.
