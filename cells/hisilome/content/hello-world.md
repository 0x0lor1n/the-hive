+++
title = "Hello, World"
date = 2025-01-01
description = "First post. Why this blog exists and what it's for."

[taxonomies]
tags = ["meta", "no-js"]
+++
This is the first post. Plain HTML, plain CSS, no build step, no JavaScript.

## Why no JS

Every script is an attack surface, a tracking vector, and a reason the page loads slower than it should. None of that is needed to publish text.

```
<p>this is just html</p>
```

New posts are new files in this directory, linked from [the archive](/) and [the feed](/rss.xml).
